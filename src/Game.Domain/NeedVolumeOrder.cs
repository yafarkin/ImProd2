namespace Game.Domain;

/// <summary>
/// Грубый порядок объёма записи на доске потребностей (SPEC §9.2: «грубый порядок объёма», не
/// точное число) — осознанно приблизительная шкала.
/// </summary>
public enum NeedVolumeOrder
{
    Small,
    Medium,
    Large
}
