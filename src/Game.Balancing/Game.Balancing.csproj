<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Game.Domain\Game.Domain.csproj" />
    <ProjectReference Include="..\Game.Config\Game.Config.csproj" />
    <ProjectReference Include="..\Game.Engine\Game.Engine.csproj" />
    <ProjectReference Include="..\Game.Bots\Game.Bots.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- /admin/balance-lab (Game.Web) переиспользует internal-логику DiagnoseRun (поиск аномалий
         себестоимости, классификация X(t)) вместо второй копии — дублирование этой самой логики уже
         один раз разошлось (fastener-plant, 2026-08-23), почин здесь, а не в двух местах сразу. -->
    <InternalsVisibleTo Include="Game.Web" />
    <!-- Юнит-тесты на саму логику вердикта (DiagnoseRun.PrintFinalVerdict и т.п.), не только на
         публичные калькуляторы. -->
    <InternalsVisibleTo Include="Game.Balancing.Tests" />
  </ItemGroup>

  <ItemGroup>
    <None Include="..\Game.Config\Samples\gameconfig.pilot.json" Link="Samples\gameconfig.pilot.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

</Project>
