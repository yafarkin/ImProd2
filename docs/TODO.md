# TODO.md — Задел на будущее (после MVP)

Открытый бэклог крупных идей, которые не тянут на блок `BUILD_PLAN.md` (тот описывает уже
реализованную пошаговую историю до пилота) и не оформлены как решённый пункт `SPEC.md` — сюда
попадает то, что обсудили, но ещё не спланировали и не начали. Мелкие незакрытые вопросы калибровки/
контента уже отдельно перечислены в `BUILD_PLAN.md` → «После BUILD_PLAN — что осталось» и в
`SPEC.md` §16 «Открытые вопросы (свод)» — этот файл их не дублирует, только даёт им один общий адрес
рядом с более крупными пунктами.

Сгруппировано по теме; порядок внутри группы — порядок появления идеи, не приоритет. Пункты
10, 14, 18, 19 и 20 предложил Claude, не пользователь, — помечены отдельно.

**Сложность** — на глаз, без декомпозиции на часы/спринты: Низкая / Средняя / Высокая.
**Важность** — мнение Claude, не решение: чем это мешает игре/пилоту прямо сейчас, если не сделать.
Обе оценки — стартовая точка для разговора, не приговор.

## Сводная таблица

| # | Пункт | Сложность | Важность |
|---|-------|-----------|----------|
| 1 | Единый бегунок сложности | Средняя | Средняя |
| 2 | Ветки технологий и связи между ними | Высокая | Высокая |
| 3 | Больше нелинейности в параметрах фабрик | Средняя | Высокая |
| 4 | Добить внешнюю экономику | Средняя-высокая | Средняя |
| 5 | Внутренняя экономика менее прибыльная | Низкая* | Высокая |
| 6 | Документ «что влияет на фабрику» + игра с моделью | Низкая* | Высокая |
| 7 | Интерфейс фабрик (мобильный, «требует внимания») | Средняя | Высокая |
| 8 | Расходящиеся цены/индикаторы прибыли | Низкая* | Высокая |
| 9 | Переосмыслить интерфейс управляющего | Высокая | Средняя-высокая |
| 10 | Экран итогов игры / дебрифа *(Claude)* | Средняя | Средняя |
| 11 | Полная админка конфига | Высокая | Средняя |
| 12 | Параметризация длительности игры → ходов | Высокая | Средняя |
| 13 | Арена ботов для прогона сценариев | Низкая-средняя | Средняя |
| 14 | Довести до конца расследование подвисаний *(Claude)* | Неизвестно | Высокая |
| 15 | Стартовая документация для игроков | Низкая | Высокая |
| 16 | Настоящий двухсторонний ввод условий контракта (живые переговоры в зале) | Средняя-высокая | Высокая |
| 17 | Упрощённая 3-секторная цепочка под тимбилдинг (75 ходов) | Средняя | Средняя-высокая |
| 18 | `IdealHallCalculator` не моделирует износ фабрик *(Claude)* | Средняя | Низкая-средняя |
| 19 | Соотношение бегунка сложности и лестницы цен *(Claude)* | Низкая-средняя | Низкая-средняя |
| 20 | LLM-боты вместо формульных *(Claude)* | Высокая | Средняя |

*Отмеченные «Низкая\*» — низкая при условии, что ясно, что именно крутить/искать; иначе выше (см.
обоснование в самом пункте).

---

## Экономика и баланс

### 1. Единый регулируемый бегунок сложности

Один ползунок сложности сессии, согласованно меняющий несколько параметров сразу — стоимость
постройки фабрик, ставку кредита, стоимость исследований. Уже отслеживается отдельно как
[[project_difficulty_slider_deferred]] — там же первый именованный параметр в эту сторону
(`growthPerLevel` лестницы цен, [[project_price_ladder_calculator]]) и второй кандидат, застолблённый
2026-08-09 в `SPEC.md` §5.10: прогрессивная стоимость владения **количеством** фабрик (после того как
появилась реальная продажа фабрики, `SellFactory`).

*Уточнено 2026-08-14* (`docs/production-balance.md` §2, «Два независимых рычага калибровки» —
пользователь явно развёл то, что раньше в этом пункте шло одним списком): общее время сессии `T` —
**не третий кандидат в этот же список**, а отдельный, ортогональный рычаг. Число ходов и минут на ход
задают *форму времени* — при одном и том же `T` разная нарезка на ходы не должна менять, докуда
доходит цепочка, только дробность решений. Бегунок сложности (этот пункт) при уже выбранном `T`
дополнительно двигает, *насколько легко или тяжело* цепочка проходится к тому же моменту — тот же
набор формул, но с другой стороны. Обе механики нужны разом и не построены ни одна: ни автоматического
вывода темпа из `T`, ни самого ползунка — пока калибровка идёт вручную на фиксированном числе ходов
конкретного пресета (`docs/balancing-tool-howto.md`).

**Сложность:** Средняя — сама механика (один множитель → несколько формул) простая, но требует
сначала свести уже существующие и будущие именованные параметры в один согласованный список.
**Важность:** Средняя — удобство настройки сессий под разную аудиторию, не блокирует текущую игру.

*Реализовано и откалибровано в сессии 2026-08-15* (`docs/difficulty.md`, все 6 шагов плана
закрыты) — не только дизайн: `DifficultyScaler` (код-константы) + `SessionConfig.DifficultyLevel`,
тесты, изолированная и совместная проверка ботами на 3 production-model файлах разной ширины
(`metallurgy.json`, `-petrochemistry.json`, `-forestry-electronics.json` — все монотонны, анкеры не
потребовали правки), UI-поле на `/session`, живой клик-тест в браузере. Осознанно исключены из
восьми рычагов: `DiminishingReturnsExponent` (слишком чувствительная степенная формула),
`growthPerLevel` (отдельный рычаг, см. №19 ниже — не сам по себе сложность, но с ней теперь надо
считаться), `TaxesConfig`/`DepositsConfig` (мёртвый код). Два рычага (`BaseLoanInterestRate`,
`EmergencyPurchaseBaseMultiplier`) не дали измеримого индивидуального эффекта на тестовом сценарии —
оставлены как есть, подробности в `docs/difficulty.md` §5 шаг 3/§6.

**Сложность:** ~~Средняя~~ — сделано.
**Важность:** ~~Средняя~~ — сделано, не открытая задача.

### 19. Соотношение бегунка сложности и лестницы цен (`growthPerLevel`) *(предложено Claude)*

Вопрос пользователя 2026-08-15, сразу после реализации №1: раз появился рычаг сложности `BasePrice`
(единый множитель цены по всем материалам), нужен ли теперь вообще `growthPerLevel`/пересчёт лестницы
цен (`/admin/price-ladder`, [[project_price_ladder_calculator]]) — и не надо ли их визуально
объединить на одной странице с явными предупреждениями о порядке.

**Что уже выяснено (не значит закрыто, значит есть с чего начинать):**
- Они решают разные задачи, не дублируют друг друга: `BasePrice` двигает *общую щедрость рынка* (все
  материалы разом, без изменения соотношений между переделами цепочки), `growthPerLevel` двигает
  *форму* — рост дохода именно вдоль цепочки переделов (тот самый экс-баг «выгоднее осесть на сырье»,
  Блок 9.4). `growthPerLevel` не становится не нужен — он чинит другую ось.
- Математически они совместимы **и не требуют пересчёта лестницы заново при каждой смене
  сложности**: `DifficultyScaler` умножает `BasePrice` всех материалов на один и тот же коэффициент,
  значит соотношение доходов между соседними переделами цепочки (`Capacity × Price × Margin`)
  сохраняется в точности — если цепочка была выровнена лестницей один раз (на любом уровне сложности),
  она остаётся выровненной на любом другом уровне автоматически.
- Из этого вытекает реальный открытый вопрос: если пересчёт и правда не нужно гонять заново при
  каждой сессии, то зачем `/admin/price-ladder` вообще сделан как *ран-тайм* инструмент администратора
  перед стартом сессии, а не как *разовый* инструмент калибровки контента (прогнать один раз при
  правке `production-models/*.json`, сохранить результат в файл, как уже сделано для
  `metallurgy.json` в Блоке 9.4)? Может быть, `/admin/price-ladder` стоит вообще убрать из
  сессионного UI и оставить только как консольный/офлайн-инструмент — тогда и порядок-зависимый
  капкан (`docs/difficulty.md` §6: обратный порядок применения стирает пересчёт лестницы без
  предупреждения) перестаёт быть проблемой сам по себе, а не только по предупреждению.
- Если всё же оставляем как ран-тайм инструмент — тогда пользователь прав, что нужно визуально
  объединить два контрола (сейчас разнесены по разным страницам, `/session` и `/admin/price-ladder`)
  и явно предупредить о порядке применения.

**Не решено:** какой из двух путей выбрать (убрать со страницы сессии vs объединить и предупредить)
— требует отдельного обсуждения с пользователем, не однозначно с ходу.

**Сложность:** Низкая-средняя — само решение не техническое, техническая часть (либо убрать страницу
из сессионного потока, либо объединить UI + текст предупреждения) в любом случае небольшая.
**Важность:** Низкая-средняя — ни то ни другое не ломает уже работающий слайдер сложности (проверено:
они математически совместимы уже сейчас), это про удобство и отсутствие путаницы у администратора,
не про корректность экономики.

### 2. Проработка всех веток технологий и связей между ними

Сейчас пирамида поколений (`GenerationResearch`) и секторные цепочки заведены, но не проработаны как
цельная система — нужно продумать все ветки технологий целиком, включая то, как они связаны между
собой (не только внутри одной ветки), а не проверять/балансировать их по одной.

**Сложность:** Высокая — это по сути дизайн контента и баланса, а не код; требует прогонов и правок
цепочек, не одной правки конфига.
**Важность:** Высокая (пользователь: «на этом строится основа игры фактически») — ветки технологий и
связи между ними это не украшение поверх готовой игры, а сам каркас, на котором держится остальная
экономика; текущие ветки играбельны (см. разбор реальной сессии на 55+ ходов, 2026-08-09), но это не
значит, что они проработаны как задумано.

*Развито в сессии 2026-08-14* (`docs/production-staging.md`): порядок раскрытия секторов
зафиксирован как 4-стадийный план (1 → 2 → 3 → 4 отрасли, глубина цепочки падает и распределяется по
ширине, а не растёт бесконечно вглубь одной ветки) — все 4 стадии реализованы файлами:
`metallurgy.json` (1 отрасль), `metallurgy-petrochemistry.json` (2, взаимная зависимость),
`metallurgy-petrochemistry-forestry.json` (3, треугольник взаимных зависимостей), и наконец
`metallurgy-petrochemistry-forestry-electronics.json` (4 — сектор Д, Электроника; впервые все
флагманы становятся настоящими готовыми продуктами, не заготовками; единственная непрямая связь во
всей лестнице — Д зависит от В только транзитивно через Б). Стоимость/пороги исследований стадий 2-3
согласованы между собой (единая сумма по хребту цепочки с стадией 1); стадия 4 — намеренно нет, по
просьбе пользователя тюнинг оставлен отдельным треком. `ProductionRate` не согласован ни у одной
стадии — как и настоящая калибровка под игровое время (`production-balance.md`), это отдельный,
ещё не начатый шаг.

*Подтверждено числами в сессии 2026-08-15* (`docs/difficulty.md` шаг 5, побочная находка при
переносе слайдера сложности на боевые файлы — не специально искали, попутно обнаружили сеткой
ботов): на `metallurgy-petrochemistry.json` (стадия 2) на baseline-калибровке (без всякого слайдера
сложности) сектор A сходится к `Score/X(T)` ≈ 0.5 (боты недобирают половину теоретического потолка),
сектор B — к ≈ 1.97 (боты почти вдвое превосходят свой потолок), независимо от стратегии бота
(leverage/profile). На `metallurgy-petrochemistry-forestry-electronics.json` (стадия 4) картина той
же формы: A≈0.51, Б≈1.58, В≈1.10, Д≈1.66. Это прямое измерение именно того разрыва, о котором
говорит абзац выше («ProductionRate не согласован») — сектора не просто «не проверены на равенство
целевой функции X(t)», а измеримо и стабильно расходятся с ней в разные стороны.

**Важное уточнение (иначе можно спутать с ожидаемой асимметрией кривой, `production-balance.md` §3):**
разная *форма* пути к результату — нормально и ожидаемо (одна ветка идёт ровно, другая рывком под
конец), но это разрыв именно в **итоговом счёте на последнем ходу** (`t = T`), где по методу
калибровки ветки должны быть сравнимы. На стадии 2 при одной и той же (нормальной, не крайней)
стратегии бота (`leverage=0, profile=0.5`) реальный итоговый счёт команды сектора A ≈ 5.3M ¤, сектора
B ≈ 25.7M ¤ — те же 90 ходов, тот же тип бота, разница в **≈5 раз**, и она не зависит от выбора
стратегии (тот же разрыв на всех 9 ячейках сетки leverage×profile). Причина в двух наложенных
эффектах разом: (1) сами потолки `X(T)` уже не равны между секторами (A=10.4M, B=13.0M — должны
были бы сойтись по методу калибровки, но пока не сведены), и (2) реальные боты сектора A недобирают
до своего потолка вдвое, а боты сектора B вдвое его превышают. Мешает не «одна ветка раньше идёт в
рост» — за 90 ходов обе ветки давно должны были сойтись, дело в самой калибровке чисел секторов
(`BuildCost`/`FixedCostPerTurn`/пороги R&D/`ProductionRate`), не в разной динамике по ходам.

*Разобрано в сессии 2026-08-15* (продолжение того же дня — пользователь попросил проверить причину
(1) на `metallurgy-petrochemistry.json`): нашли и исправили две отдельные, наложенные друг на друга
проблемы, обе подтверждены прогонами `Game.Balancing` (750 партий на ячейку сетки):

- **Причина (1), потолок `X(T)`, — конфиг:** рецепт `oil-drilling` кормит четыре нисходящих рецепта
  (`gasoline`/`ethylene`/`synthetic-rubber`/`carbon-black`), а не один, как любой другой сырьевой
  рецепт в обоих секторах — `ProductionRate` был откалиброван под совокупный спрос всех четырёх
  (16970, на порядок выше соседей). Бисекцией через `--mode ideal-hall` нашли и применили значение,
  сводящее `X(90)` секторов к паритету — коммиты `d9bda4e`/`f4f9b06` (см. ниже, почему число менялось
  дважды).
- **Причина (2), реальные боты вдвое мимо потолка, — не конфиг, баг самого `IdealHallCalculator`:**
  модель X(t) никогда не продавала излишек системе за деньги — только пассивно оценивала остаток
  склада по плоской `BasePrice` в конце хода, без наценки `MarginMultiplierByProcessingLevel`.
  Реальный `SimpleBot.SellSurplusToSystem` так не делает — продаёт весь несвязанный излишек каждый
  ход по настоящей котировке с наценкой уровня передела. Ветвистые цепочки (сектор B — `oil`/`ethylene`
  расходятся на 3-4 параллельных передела) теряли в модели куда больше немонетизированного излишка,
  чем линейные (сектор A) — отсюда и устойчивый разрыв `Score/X(T)`, не зависящий от `ProductionRate`.
  Исправлено в `IdealHallCalculator` (коммит `ddb4fc0`, добавлен тест, доказывающий, что наценка
  теперь реально участвует в расчёте) — после этого пришлось пересчитать `oil-drilling` заново
  (12971 → 14400, коммит `f4f9b06`): число, откалиброванное под старую, ошибочную модель, перестало
  давать паритет на исправленной.

**Итог после обеих правок:** потолок `X(T)` сходится почти точно (B/A = 0.999×, было 1.25×). Но
реальные деньги ботов **всё ещё расходятся почти так же, как раньше** — в среднем по сетке B/A ≈
2.23×, на консервативной стратегии (`leverage=0`) ≈ 3.86× (было 2.88×/4.90× до всех правок — разрыв
не в пределах шума, но и не закрылся). При этом абсолютные числа сходимости изменились сильно (A:
0.50→0.20, B: 1.97→0.76 — потолок вырос почти втрое у обоих после учёта наценки, реальные деньги
ботов почти не изменились), значит и старое, и новое совпадение по величине — не случайность, а
устойчивое свойство. **Вывод: оставшийся разрыв — не про `ProductionRate`/конфиг и не про модель
идеального зала (та теперь измеряет верно), а про то, что `SimpleBot` реально реализует только ~20%
своего потолка в линейной цепочке A и ~76% в ветвистой B** — разное поведение бота (или его пригодность)
в зависимости от формы цепочки сектора. Не исследовано, почему именно — отдельная задача, возможные
направления: жадные эвристики продажи/покупки бота хуже работают на цепочке с одним потребителем на
передел, чем на цепочке с параллельными ветвлениями; тайминг построек/R&D; контестация ёмкости рынка
между несколькими командами одного сектора (`--teams-per-sector`, в идеальном зале контестации нет
вообще, там одна ветка на сектор).

*Разобрано дальше в той же сессии 2026-08-15 — найдены и починены две структурные причины разрыва*
(не про `Score/X(T)` конвергенцию саму по себе, а про то, почему у сектора A она вообще низкая):

1. **Флагман A — самая широкая точка слияния во всём графе.** `automobile-assembly` требует 7
   одновременных входов (welded-body/gearbox-assembly/engine/fasteners/tires/paint/gasoline) —
   больше любого другого узла обоих секторов; `boat-assembly` — только 5. Чем больше независимых
   условий нужно свести в один ход без явной синхронизации у `SimpleBot`, тем ниже шанс, что сборка
   вообще сработает — непочиненная гипотеза, не проверена экспериментально (осталось на будущее).
2. **Новая, четвёртая ось баланса — торговый поток между секторами (см. `docs/production-balance.md`
   §1).** Разложение обоих флагманов по спецификации (BOM) до материалов другого сектора показало:
   `automobile` ввозил из B 6723 (цена×наценка) ценности на единицу, `boat` из A — только 4240 (1.59×
   перекос), на 94% из-за одного входа (`tires: 4`, тянет 5-уровневую резиновую цепочку с
   накопленным множителем количества ~94×). Проверенный тупик: пересчёт единой лестницы цен
   (`SystemSalePriceLadderCalculator`, `growthPerLevel=1.2`) решает *другую* проблему (доход-на-уровень
   у B был systematически выше A на каждом переделе — тоже подтверждено и почему-то не то же самое,
   что торговый перекос) и даже слегка ухудшает торговый баланс (1.59× → 1.83×) — не трогать цену,
   если нужен именно торговый баланс. Рабочий фикс (по указанию пользователя, с реальным
   обоснованием — лодка/яхта должна требовать заметно больше металла, чем машина): `steel-stock` в
   `hull-from-composite-and-steel` 2 → 39 (бисекция под целевой паритет), плюс каскадный пересчёт
   всей вышестоящей цепочки стали (`iron-ore-mining` и далее) под новый спрос с тем же запасом ×1.3,
   что и везде. Потребовал повторной бисекции `oil-drilling` (14400 → 19000 — свежий скачок сырьевого
   потока в A сдвинул и его собственный потолок).

**Итог обеих правок, проверено сеткой ботов (5×5, 30 партий/ячейка, 750 партий):** реальный разрыв
денег сузился с 2.23×/3.86× (среднее/консервативная стратегия) до **1.81×/2.71×** — конвергенция A
выросла с ~20% до ~28% на `leverage=0`, это настоящий эффект, не просто сдвиг потолка (сектор B не
изменился вообще). **Разрыв не закрыт полностью** — оставшаяся причина, скорее всего, гипотеза (1)
выше (ширина слияния флагмана), не проверена. Коммит: `0530fd8` (число `oil-drilling`/`steel-stock`
+ каскад ProductionRate + новая ось в `production-balance.md`).

*Гипотеза (1) («ширина слияния флагмана») проверена и опровергнута экспериментом* — сократил
`automobile-assembly` с 7 до 5 входов (убрал `paint`/`gasoline`, потолок почти не сдвинулся), прогнал
те же 750 партий — **никакой разницы** (28,0%/28,1% сходимости A что до, что после, в пределах шума).
Число входов у флагмана не влияет на конвергенцию `SimpleBot` вообще.

**Вместо неё нашлась и подтверждена настоящая причина — асимметрия поколений исследования, не бот.**
Смотрите на сходимость по всей сетке `leverage`/`profile` (тот же прогон 750 партий): при
`leverage=1, profile=0` (вложение в исследование поколений со старта) A даже ОБГОНЯЕТ B (84,6% против
64,9%). При `leverage=0` или `profile=1` (вложение поздно/никогда) разрыв возвращается к ×2,7. Значит
дело не в самом боте — дело в том, *насколько конфиг штрафует задержку исследования поколений
по-разному для A и B*. Посчитал независимо от бота: суммарную «доступную ценность» (фабрики × ёмкость
× цена × наценка) по каждому поколению разблокировки — на поколении 1 (до всякого исследования) у B
уже в 1,63 раза больше доступно, чем у A (пик — 1,81× на поколении 2), паритет только к поколению 6-7.
Причина: у B пять материалов уровня 1 (прямо из нефти/газа: `gasoline`/`ethylene`/`synthetic-rubber`/
`methanol`/`carbon-black`), у A — только два (`coke`/`wire-rod`, оба целиком уходят в серийную
цепочку стали, ничего не продаётся сразу). **Это реальный риск для живого пилота на 200 человек** —
живая команда, которая не бросится агрессивно вкладывать в исследование поколений с 1-го хода
(реалистичное поведение для большинства), окажется в структурно худшем положении в секторе A не по
своей вине.

**Исправлено (коммит `ad7d00e`):** добавлены три тупиковых материала уровня 1 сектору A — по образцу
пяти у B, реальные металлургические побочные продукты, не трогающие существующую (уже
откалиброванную) серийную цепочку стали:
- `quicklime` (Известь негашёная) из `limestone`, фабрика `lime-kiln`;
- `coal-tar` (Каменноугольная смола) из `coking-coal`, фабрика `coal-tar-plant`;
- `iron-powder` (Железный порошок) из `iron-ore`, фабрика `iron-powder-plant`.

Подобраны по цене/ёмкости под уровень B (~4000-4500 ценности каждый, ~12 500 суммарно) — сводят
поколение 1 к точному паритету (1,00×, было 1,63×), поколение 2 — к 1,40× (было 1,81×). Тот же каскад,
что и раньше: новые рецепты забрали запас у `iron-ore`/`coking-coal`/`limestone` (был впритык ×1.3 под
старую цепочку) — пересчитаны заново под новый спрос, и `oil-drilling` перебисектирован в третий раз
(19000 → 25900, потолок A подскочил с 37,9М до 52,4М из-за трёх новых источников дохода).

**Итог всех трёх правок (сравнение с самой первой находкой сессии):** реальный разрыв денег ботов
сузился с исходных 2.9×/4.9× до **1.50×/1.90×** (среднее/консервативная стратегия). Сходимость A на
`leverage=0` выросла с исходных ~20% до **38,7%**. Разброс сходимости внутри сетки тоже заметно
сузился (~35% против ~48% до этой правки) — экономика в целом стала предсказуемее, не только
средние числа сблизились. **Разрыв всё ещё не закрыт полностью** — остаточная причина не
идентифицирована, но доминирующий вклад (асимметрия поколений) устранён.

*Ещё три раунда той же сессии — две гипотезы опровергнуты, инструмент подтверждён чистым, идея
пользователя реализована отдельно.*

1. **«Скидка за перепроизводство» (рынок ограничивает выручку сырой нефти) — опровергнута.** Урезал
   `Capacity` нефти в 8 раз (4000→500) — разрыв не изменился (1,89×/1,49× вместо 1,90×/1,50×).
   Причина: у сектора B две команды делят одну и ту же ёмкость рынка, обе вместе выкачивают нефть
   далеко за пределы любой разумной ёмкости в любом случае — механизм скидки уже насыщен, дальше
   урезать некуда.
2. **«Один большой кран (нефть) против четырёх маленьких (руда/уголь/известняк/лом)» — тоже
   опровергнута, но заодно реализована независимая идея пользователя (единые нормируемые единицы
   счёта).** Свёл каждый сектор к ровно одному сырью — `iron-ore` (переименован в «Руда», поглотил
   `coking-coal`/`limestone`/`scrap-metal`) и `oil` (поглотил `natural-gas`) — коммит `7308828`.
   Разрыв не изменился (1,92×/1,50× — статистически то же самое, что и с четырьмя сырьями). Оставлено
   в файле — не из-за баланса, а потому что даёт то, что реально просили: «автомобиль стоит N руды,
   катер стоит M нефти» одним числом, без взвешивания по нескольким материалам.
3. **Контрольный эксперимент — механизм (не конфиг) подтверждён чистым, коммит `c53c812`.**
   Собрал `control-twin-metallurgy.json` — полная копия `metallurgy.json`, продублированная в два
   зеркально одинаковых сектора (вторая ветка — суффикс `-2`). Идеальный зал: X(t) совпадает
   побайтово. Сетка ботов без всяких связей: `AverageFinalConvergenceSpread` = ровно 0,0% на всех
   25 ячейках. Добавил 3 симметричные межсекторные связи (одинаковое количество/уровень в обе
   стороны, с реальным дефицитом 0,89-0,97× — торговля через биржевой стакан реально нужна, не
   формальность) — снова ровно 0,0% на всех ячейках. **Значит ни `SimpleBot`, ни `IdealHallCalculator`,
   ни `StrategyGridRunner`, ни биржевой стакан не отдают предпочтение «первому» или «второму» сектору
   сами по себе** — весь разрыв, который нашли и почини­ли в этой сессии (торговый баланс, асимметрия
   поколений), был настоящим содержательным багом конфига, не артефактом инструмента. Оставленный
   разрыв (1,50×/1,90×) в `metallurgy-petrochemistry.json` — тоже конфиг, просто ещё не найденный.
   Закреплено постоянным регрессионным тестом (`SectorSymmetryRegressionTests`, `Game.Bots.Tests`,
   ~0,4с) — если будущая правка `Game.Balancing`/`SimpleBot`/`IdealHallCalculator` случайно внесёт
   асимметрию по порядку секторов, этот тест покраснеет первым, раньше, чем кто-то снова часами будет
   искать баг в конфиге вместо инструмента.

**Статус на конец сессии 2026-08-15:** разрыв денег ботов сектора A/B в `metallurgy-petrochemistry.json`
сужен с 2.9×/4.9× до 1.50×/1.90× — два реальных бага конфига найдены и исправлены (торговый баланс,
асимметрия поколений), три структурные гипотезы проверены и опровергнуты (ширина слияния флагмана,
скидка за перепроизводство, один сырьевой кран), механизм (не содержание) подтверждён экспериментом
как чистый от асимметрии. Оставшаяся причина разрыва не идентифицирована — не установлено, стоит ли
продолжать поиск или считать этот уровень остатка приемлемым для пилота.

**Решение по дальнейшему поиску (зафиксировано тем же вечером):** не продолжать вручную перебирать
структурные гипотезы формульным `SimpleBot`/`Game.Balancing` — три подряд не подтвердились, дальше
это, похоже, требует построчного разбора конкретных узлов цепочки, а не общих теорий. Детальную
проверку остаточного разрыва (1,50×/1,90×) отложить до появления LLM-ботов (пункт 20) — они
дают качественный, а не только числовой, взгляд на то, где именно экономика ощущается «тесной» в
одном секторе и «просторной» в другом, и заодно закрывают пробел `Factory.SelectRecipe`
([[project_multi_recipe_factory_gap]]), которого формульному боту всё равно не хватает для честной
проверки многорецептурных развилок. До появления LLM-ботов `metallurgy-petrochemistry.json`
остаётся в текущем, не идеально сбалансированном, но играбельном состоянии; стадии 3/4 по той же
причине не переаудированы.

**Стадия 1 (`metallurgy.json`) переаудирована — правка `ProductionRate` НЕ нужна, это отрицательный,
но проверенный экспериментом результат.** `--mode ideal-hall`: X(90) = 54,1 млн (тот же ~3-кратный
скачок потолка, что и на стадии 2, от активной продажи излишка по марже). `--mode grid` (5×5, 30
партий/ячейка): сходимость 14,0% на leverage=0 (плоско по всем profile) до 73,7% на leverage=1/profile=0
— на первый взгляд тревожно низко, напросился вывод «поднять ProductionRate». Проверил прямым
экспериментом, не гаданием: умножил все `ProductionRate` цепочки на K=2 (пропорции headroom не
трогает) — X(90) вырос до 95,5 млн как и ожидалось, а сходимость почти не сдвинулась (14,2% / 77,7%).
**Значит равномерный подъём `ProductionRate` в принципе не может подвинуть сходимость** — идеальный зал
считает свой потолок по тем же самым числам, что и реальный бот, числитель и знаменатель растут
вместе. Причина низкой сходимости — тот же поведенческий разрыв бот/идеальный зал, что уже
задокументирован на стадии 2 (`SimpleBot` при leverage=0 вообще не инвестирует в исследование
поколений и навсегда остаётся на поколении 1 — отсюда плоские 14% по всем profile), только заметнее,
потому что цепочка стадии 1 длиннее (9 уровней против 7 у стадии 2). Рычаги, которые подняли сходимость
на стадии 2 (правка торгового баланса, асимметрии поколений между секторами), у стадии 1 неприменимы —
сравнивать не с чем, сектор один. Оригинальный критерий калибровки 2026-08-14 (флагман достижим, X(t)
растёт гладко, не застревает в бэклоге сырья) по-прежнему выполняется чисто. Ось (a) для стадии 1
закрыта, ось (b) неприменима по построению (один сектор). **Стадия 1 больше правок не требует.**

**Стадия 3 (`metallurgy-petrochemistry-forestry.json`) переаудирована — тот же баг генерации-1, что и
на стадии 2, найден и починен тем же способом, плюс проверка теперь встроена в инструмент.** Прогон
показал знакомую картину: потолок X(T) почти паритетный (A=26,48М/B=23,45М/V=27,13М, max/min=1,16×),
но реальные деньги ботов — нет: сектор B систематически обгонял A и V (в среднем 2,81×/2,37×, до
3,90× на некоторых ячейках), особенно резко при `leverage=0` (B=86% сходимости против A=20%/V=26%).
Причина оказалась той же самой, что уже чинили на стадии 2 (`docs/production-balance.md` §1,
«Генерация-1»), просто её никогда не переносили в этот отдельный файл: у B было 5 материалов уровня 1
(`gasoline`/`ethylene`/`synthetic-rubber`/`methanol`/`carbon-black`) против 2 у A и 2 у V — а
`SimpleBot` при `leverage=0` навсегда остаётся на стартовом поколении. Проверено количественно
(«ценность на старте» = `Σ BaseCapacity × BasePrice`): B=24700, A=7800, V=9800 — разрыв 2,5-3,2×.

Правка: добавил по 3 новых тупиковых материала уровня 1 сектору A (`quicklime`/`coal-tar`/
`iron-powder`, тот же рецепт, что и на стадии 2) и сектору V (`sawdust`/`tree-resin`/`cotton-seed`,
новые побочные продукты `timber`/`raw-cotton`), подобрав `BaseCapacity`/`BasePrice` так, чтобы выйти
на точный паритет (A=24800, B=24700, V=24600). Каскадом пересчитал `ProductionRate` сырья
(`iron-ore-mining`/`coking-coal-mining`/`limestone-quarrying`/`logging`/`cotton-picking`) под новый
спрос (headroom 1,30× везде), затем перебисектил `oil-drilling` (12971 → 22800), поскольку потолки A
и V подскочили (26,48М/27,13М → 43,80М/44,29М) и B нужно было подтянуть следом — итоговый X(T)
max/min=1,011×. Проверено сеткой (5×5, 30 партий/ячейка, 750 партий): разрыв реальных денег сузился
2,81×/2,37× → **1,71×/1,68×** (B/A, B/V), сходимость A при `leverage=0` выросла вдвое (19,7% → 38,7%),
V — в 1,6 раза (25,7% → 41,6%). Тот же порядок улучшения, что и на стадии 2 в аналогичной точке
процесса. Полный набор тестов (`Game.Config.Tests`/`Game.Web.Tests`/`Game.Engine.Tests`/
`Game.Bots.Tests`, 657 тестов) зелёный.

**Проверка встроена в `Game.Balancing` — `GenerationParityCheck`, печатается автоматически при каждом
прогоне с ≥ 2 секторами, до долгой сетки.** Считает ту же «ценность на старте» и предупреждает при
разрыве > 1,5×. Немедленно окупилась: прогон на стадии 4
(`metallurgy-petrochemistry-forestry-electronics.json`) сразу показал **тот же баг там тоже**
(B=24700, A=12000, D=11100, V=9800 — разрыв 2,52×), ровно то, что подозревал пользователь («в 4
отраслях вообще плохо будет») — подтверждено за секунды, не часами сетки ботов.

**Стадия 4 починена тем же способом (следующий ход той же сессии).** Добавил A два новых материала
уровня 1 (`quicklime`/`coal-tar`, как на стадиях 2-3), V — три (`sawdust`/`tree-resin`/`cotton-seed`,
как на стадии 3), D — два новых (`niobium-powder` из `rare-metal-ore`, `silica-fume` из `silicon`,
реалистичные побочные продукты добычи редких металлов/кремния). Вышло на точный паритет ценности на
старте (B=24700, D=24700, A=24600, V=24600 — предупреждение больше не срабатывает). Это резко подняло
потолки A/V/D (до 38,7М/33,1М/27,1М при неизменном B=22,4М) — понадобилась многопараметрическая
бисекция (каждый сектор получил свой общий множитель на все свои сырьевые `ProductionRate`, не только
одну «трубу», как раньше у двух-трёхсекторных файлов — здесь у каждого сектора это единственный
практичный рычаг, раз добавка задела разные узлы) — 12 итераций до X(T) max/min=1,016× (все 4 сектора
в пределах ~32М). Сеткой (5×5, 30 партий/ячейка) подтверждено: остаточный разрыв реальных денег сел в
тот же диапазон, что и у стадий 2/3 после аналогичного фикса — **в среднем 1,63×, максимум 1,94×**
(было ~300-530%/680-1740% по старой, не самой сравнимой метрике из калибровки 2026-08-14 —
до фикса `IdealHallCalculator`). Сходимость при `leverage=0` заметно выросла у всех отстающих
(A 20%→35%, D — аналогично). Полный набор тестов (657) зелёный. **Все 4 стадии теперь на одном и том
же уровне остаточного разрыва** (1,5-1,9×) — дальнейший поиск причины по всем четырём отложен до
LLM-ботов, тем же решением, что и выше.

**Остаётся по этому пункту (на конец сессии 2026-08-15):**
1. Причина остаточного разрыва 1,5-1,9× на всех 4 стадиях не найдена — три структурные гипотезы
   опровергнуты, поиск сознательно остановлен, ждём LLM-ботов (пункт 20).
2. Кросс-секторная ось торгового баланса (`production-balance.md` §1, «Кросс-секторная») проверена и
   исправлена только один раз — на стадии 2, пара автомобиль/лодка (`steel-stock`). Стадии 3 и 4 по
   этой оси не проверялись вообще (в отличие от генерации-1, для которой есть `GenerationParityCheck`,
   для торгового баланса автоматической метрики в `Game.Balancing` до сих пор нет).
3. LLM-боты не начаты (пункт 20, полный дизайн уже записан) — к нему привязаны пункты 1 и 4.
4. `Factory.SelectRecipe` ([[project_multi_recipe_factory_gap]]) — ни один формульный бот его не
   вызывает, многорецептурные развилки формульно не проверить; тоже ждёт LLM-ботов.

### 3. Больше нелинейности в параметрах фабрик

Продолжение уже начатой линии ([[feedback_spiral_economy_design]] — прогрессивная зарплата,
нелинейное исследование поколений): R&D на высоких уровнях фабрики должно стоить дороже, стоимость
рабочих — расти с их числом, и так далее по другим параметрам фабрик, где сейчас зависимость плоская
там, где по духу экономики должна быть растущая.

**Сложность:** Средняя — расширение уже существующего паттерна, но осмысленно только после пункта 6
(документ модели) — иначе это гадание, какие именно параметры и как гнуть.
**Важность:** Высокая — прямое продолжение уже выбранной философии «спиральной экономики»; без этого
пункт 5 (менее прибыльная экономика) труднее сделать красиво, не грубым урезанием чисел.

### 4. Добить внешнюю экономику

Три связанных куска одной задачи:
- Догенерировать библиотеку новостных заголовков (`NewsCalculator`, Блок 6.3 `BUILD_PLAN.md`) —
  SPEC §13 просил 30-50, теперь речь про ближе к сотне «фраз».
- Проверить, как внешняя экономика (тренд → цена/ёмкость рынка, §5.4) реально влияет на игру по
  факту, не только по формуле на бумаге.
- Допроектировать и реализовать её детальнее — вместе с Claude, а не в одиночку с ходу; отдельный
  заход на проектирование, не точечная правка.

**Сложность:** Средняя-высокая — заголовки сами по себе низкая сложность (контент), но переосмысление
функции «тренд→цена/ёмкость» — отдельное проектирование и калибровка.
**Важность:** Средняя — рынок сейчас работает и не ломает игру, это про глубину и убедительность, не
про починку сломанного.

### 5. Сделать внутреннюю экономику менее прибыльной

Сейчас деньги в игре зарабатываются слишком легко — команда может не особо напрягаться и всё равно
быть в плюсе. Смежно с пунктом 3 (нелинейность) и пунктом 1 (сложность), но отдельный вопрос: дело не
только в форме кривых, а в том, что общий уровень прибыльности сейчас настроен слишком щедро.

**Сложность:** Низкая, если понятно, что именно крутить (числа в конфиге) — но легко испортить баланс
не глядя, поэтому по факту требует прогонов (см. пункт 13, арена ботов) для проверки, не сломали ли
где-то ещё.
**Важность:** Высокая — сейчас, по словам пользователя, зарабатывать легко и напрягаться не нужно —
это подрывает саму идею игры «принимать решения под давлением».

### 6. Документ «что влияет на работу фабрики» + отдельная игра с этой моделью

Собрать в одном документе полную модель того, что определяет работу фабрики: рабочие и их
зависимость от количества (прогрессивная зарплата), капремонт, R&D, вложения и т.д. — сейчас это
разбросано по коду и головам. После этого — отдельно поиграть с этой моделью: что-то сделать
линейным, что-то нелинейным, что-то убрать, что-то ввести.

**Сложность:** Низкая для самого документа (свести уже существующее и уже понятное в коде), открытая
для последующей игры с моделью — там сложность зависит от того, что решат менять.
**Важность:** Высокая — прямая предпосылка для пункта 3: нельзя осмысленно решить, где добавить
нелинейность, не видя всю модель сразу, а не по одному параметру за раз.

### 17. Упрощённая 3-секторная цепочка под тимбилдинг (75 ходов)

Сессия 2026-08-15 (продолжение калибровки лестницы стадий, `[[project_production_staging_plan]]`):
обсудили два реальных формата мероприятия и выяснили, что им нужны РАЗНЫЕ production-model файлы, не
один и тот же с разной длительностью хода:

- **Целый день со студентами** (3ч игры + перерыв + 3ч игры = 6ч чистой игры, 80-200 человек) —
  закрывается уже готовой, откалиброванной сессией 2026-08-14 экономикой `metallurgy-petrochemistry-
  forestry-electronics.json` (4 сектора, 90 ходов) как есть — 360 мин ÷ 90 ходов = 4 мин/ход, с
  запасом. Новый файл не нужен.
- **Тимбилдинг** (3ч = 1.5ч игры + 0.5ч пауза + 1.5ч добивка, 20-25 человек, 8-12 команд) — для этого
  нужен ОТДЕЛЬНЫЙ, более простой файл: копия `metallurgy-petrochemistry-forestry.json` (стадия 3, уже
  3 сектора), с урезанной на 1-2 уровня глубиной фабрик и пересчитанным под **75 ходов** темпом
  (180 мин ÷ 75 ходов = 2.4 мин/ход).

Причина не просто растянуть таймер хода на уже готовый файл (что математически тоже сходится: 2 мин ×
90 ходов = 180 мин): аудитория одноразовая (первый и, скорее всего, последний раз), не окупает
глубину, которая раскрывается только повторной игрой — растянутый таймер даёт больше времени
разглядывать те же 4 сектора/50 материалов, но не убирает саму сложность. Плюс менее очевидный, но
конкретный довод — с 8-12 командами на 4 сектора выходит по 2-3 команды на сектор; на 3 сектора — по
3-4. Меньше команд на сектор = больше риск, что один запутавшийся новичок обрушит экспорт всего
сектора соседям (ровно то, что сессия 2026-08-14 нашла и почитала эмпирически на секторе Д стадии 4 —
хрупкость тонкой межсекторной торговли, там с ботами, тут будет с реальными людьми без опыта).

**Не начато** — только согласован план (75 ходов, 3 сектора, копия стадии 3 минус 1-2 уровня), сама
цепочка ещё не собрана.

**Сложность:** Средняя — не с нуля (копия уже откалиброванного файла), но урезание уровней требует
пересборки части рецептов/фабрик и полного повтора цикла калибровки этой сессии (`--mode ideal-hall` →
дефицит-лист → сетка ботов), не просто смена одного числа.
**Важность:** Средняя-высокая — блокирует конкретный реальный формат мероприятия (тимбилдинг), но не
блокирует уже рабочий пилот/полный день.

---

## Интерфейс и опыт игроков

### 7. Доработка интерфейса по фабрикам (в первую очередь мобильный)

Сейчас неудобно и не видно, какие фабрики требуют внимания прямо сейчас (низкое `Condition`,
нехватка сырья на входе, простаивающие рабочие) — нужен явный сигнал/сортировка по «требует
внимания», а не только цвет рамки на карточке. Пользователи заходят с телефонов с небольшим экраном
и не должны бороться с интерфейсом, чтобы найти проблемную фабрику. Конкретный пример из разбора лога
2026-08-09: `electric-goods-plant` простояла 25 ходов подряд с нулевым выпуском из-за нехватки
сырья — на экране это было видно только как мелкая красная подпись, легко пропустить.

**Сложность:** Средняя — не требует изменений в движке, только новый расчёт «что требует внимания» и
разметка поверх уже существующих данных `FactoryRow`.
**Важность:** Высокая — уже пойман конкретный случай реальной потери 25 ходов производства незамеченной;
прямое влияние на то, играбельна ли сессия для участников с телефонов.

### 8. Разобраться с отображаемыми ценами/индикаторами прибыли

Сейчас часть интерфейса продолжает показывать «уходите в минус», когда по факту команда в большом
плюсе — какой-то из показателей (цена/ставка/индикатор прибыли) разошёлся с реальным финансовым
положением и вводит в заблуждение. Нужно найти, какой именно экран/показатель это делает, и
разобраться, показывает ли он то, что должен.

**Сложность:** Низкая, как только найдём конкретный экран/показатель — сама находка неизвестной
длительности, это не рефакторинг, а точечный баг.
**Важность:** Высокая — вводит игроков в заблуждение прямо сейчас; доверие к цифрам — это основа
принятия решений в игре, разошедшийся индикатор подрывает именно его.

*Найдено и починено в сессии 2026-08-16* — пользователь подтвердил экран: виджет «Прибыльность
фабрики» (`FactoryProfitabilityCalculator`, вкладка «Прибыльность» на `/team`). Причина —
`totalTeamWorkers` (пул для прогрессивной командной зарплаты, `FinanceCalculator.CalculateSalaries`)
суммировал рабочих **всех** фабрик команды, включая те, что на вынужденном простое, а реальный расчёт
хода (`TickFinanceStep.Run`) явно исключает простаивающие фабрики из этого пула (их зарплата и
содержание списываются отдельно, плоским льготным тарифом простоя, см. `WearStep.RunRepairTurn`) — то
есть команда с любой одной простаивающей фабрикой видела у ВСЕХ остальных, работающих фабрик
завышенную (по более высокой прогрессивной ступени) зарплатную нагрузку в этом виджете, из-за чего он
мог показать убыток там, где реальный тик списал бы меньше и команда была в плюсе. Заодно поправлена
симметричная ошибка: сама простаивающая фабрика в виджете тоже оценивалась по обычной
(прогрессивной/полной) ставке, а не по льготному тарифу простоя (`RepairSalaryRate`/`RepairUpkeepRate`).
Исправлено в `FactoryProfitabilityCalculator.TryCalculate` (`src/Game.Engine`), два новых теста
(`TryCalculate_Excludes_Factories_Under_Repair_From_The_Progressive_Wage_Pool`,
`TryCalculate_Uses_The_Repair_Tariff_For_A_Factorys_Own_Wage_And_Upkeep_While_Under_Repair`), полный
набор тестов решения зелёный (487 в `Game.Engine.Tests`).

**Сложность:** ~~Низкая~~ — сделано.
**Важность:** ~~Высокая~~ — сделано, не открытая задача.

### 9. Переосмыслить интерфейс управляющего

Сейчас на `/team` для роли «Управляющий» всё смешивается в одну кучу — фабрики, склад, R&D, кредиты,
контракты выросли органически, вкладка за вкладкой, и общая структура экрана за этим не поспевает.
Шире, чем пункт 7 (тот — про то, как быстро увидеть проблемную фабрику именно на мобильном); этот —
про информационную архитектуру экрана управляющего в целом.

**Сложность:** Высокая — `Team.razor` уже большой, органически выросший файл; это полноценный
редизайн, не патч поверх существующей структуры.
**Важность:** Средняя-высокая — реальная, названная пользователем боль, но не блокирует игру технически
(решения всё ещё можно принять), только удобство и скорость.

### 10. Экран итогов игры / дебрифа *(предложено Claude)*

`SPEC.md` §12 ещё с MVP анонсирует «(после MVP) HTML-отчёт с графиками ... режим replay», но сейчас,
когда `GameSessionState.IsFinished` становится true, для игроков и ведущего физически негде увидеть
финальные счета команд — `FinalScoreCalculator` есть и используется harness'ом балансировки, но не
показан ни на одном экране. Отдельный экран (или блок на большом экране/`/team`) с итоговым счётом
по всем командам и графиками истории по ходам закрыл бы этот разрыв.

**Сложность:** Средняя — расчёт уже готов и чист (`FinalScoreCalculator`), нужен в основном экран и,
может быть, график.
**Важность:** Средняя — красиво закрывает сессию и даёт материал для разбора после игры, но не
блокирует саму игру, пока она идёт.

### 16. Настоящий двухсторонний ввод условий контракта (не только подтверждение)

2026-08-11 закрыт более острый баг: одна команда могла и создать, и подтвердить контракт сама,
контрагент вообще не участвовал (`ConfirmContract` теперь требует, чтобы подтверждал именно
контрагент, не инициатор — коммит `0bdffd9`). Но само «сведение независимых заявок» из
`SPEC.md` §6 («обе команды вводят условия независимо → система сверяет совпадение») по-прежнему
не настоящее: `ContractDraftForm.razor` (`SubmitDraft`) сам фабрикует вторую заявку от имени
контрагента с теми же условиями, что ввела первая команда, вместо того чтобы контрагент реально
ввёл свои условия и система сверила их с чужими. Отдельного экрана «подтверждение встречного
ввода», который `SPEC.md` §9.3 закладывает в списке функций дашборда, в коде нет вовсе.

Пока намеренно оставлено как есть — удобно для отладки механизма — но пользователь отметил
(2026-08-11), что это не только «защита от опечаток». Ключевой принцип SPEC (стр. 13, 43):
«переговоры и заключение контрактов происходят вживую (люди перемещаются по залу, фиксируют
сделки на бумажных бланках)» — управляющий сидит на месте, переговорщики ходят по залу и
договариваются лично. Текущий цифровой черновик обходит это целиком: одна команда может
единолично «настрогать» контрактов-офферов на кучу других команд удалённо, без какого-либо
реального личного контакта — контрагент просто видит готовый контракт и жмёт «подтвердить». Это
убивает именно офлайн-механику зала, а не только страхует от разночтений условий. Также сейчас
у команды-получателя на `/team` нет кнопки «отклонить» саму заявку (только оператор может через
`RejectContract`) — см. обсуждение того же дня.
**Как переделать — не решено, требует дизайна** (нужно понять, как заставить обе стороны физически
согласовать условия до/во время цифрового ввода, а не просто честно продублировать поля ввода).

**Сложность:** Средняя-высокая — не просто новый экран для встречного ввода, а вопрос дизайна
самого процесса «заставить стороны реально встретиться», не только техническая реализация.
**Важность:** Высокая — задевает базовый принцип SPEC (живые переговоры в зале), а не только
защиту от опечаток.

---

## Инструменты и инфраструктура

### 11. Полная админка конфига

Экран (или отдельный проект), позволяющий видеть и редактировать **всё**, что сейчас есть в
`GameConfig` — не только выборочные инструменты вроде `/admin/price-ladder`
([[project_price_ladder_calculator]]), а весь конфиг целиком, из одного места, до старта сессии и
(там, где это осмысленно) во время неё. `/admin/price-ladder` — уже готовый прецедент такого рода
инструмента, можно ориентироваться на его подход (пересчёт цепочкой от одной ручки, а не ручная
правка каждого числа).

**Сложность:** Высокая — `GameConfig` большой и разнородный (17+ разделов), нужна валидация не хуже,
чем у `GameConfigLoader`; вероятно, отдельный UI-слой или даже отдельный проект, как и предположил
пользователь.
**Важность:** Средняя — полезно для скорости итерации по конфигу, но самые частые точечные правки уже
закрыты (`/admin/price-ladder`); можно жить без него дольше многих других пунктов.

### 12. Параметризация длительности игры (минуты) → число ходов

Сейчас пресеты сессии задают число ходов и длительность фаз раздельно. Хочется задавать **длительность
игры в минутах** и **максимальное число ходов** (помня про закрытый финал — сколько ходов реально
понадобится, заранее неизвестно), а длительность хода (и всё, что от неё зависит) — выводить из этих
двух чисел, а не подбирать вручную. Пример из обсуждения: 4 часа / 100 ходов ≈ 2.4 минуты на ход
(решения чаще, цена ошибки за один ход ниже); те же 4 часа / 30 ходов ≈ 8 минут на ход, считая фазу
расчёта (решений меньше, но каждое весомее) — и прогресс экономики за один ход должен в этом случае
идти примерно втрое быстрее, чтобы сессия всё равно ощущалась завершённой к концу отведённого времени.
Требует продумать, какие константы конфига (проценты за ход, скорость исследований, износ) должны
сами масштабироваться от выбранного числа ходов, а не оставаться абсолютными.

*Развито в сессии 2026-08-13* (`docs/production-balance.md`): открытый вопрос выше решён на уровне
метода, не чисел — мастер-параметр это общее время `T` в минутах, число ходов производная от него и
желаемой дробности решений; формулы темпа экономики должны быть функцией от `T`, не константой на
пресет. Сами числа/формулы всё ещё не откалиброваны.

**Закрыто 2026-08-15 — решили не строить автовывод, оставили ручную калибровку.** Метод остался
только на бумаге: не найдено ни одного места в коде, где `T = MaxTurns × TurnDurationMinutes`
вычисляется и используется для программного масштабирования констант — только один ручной расчёт на
бумаге (`docs/production-staging.md`, формула `MaxCommitmentPerTurn = порог²/T`, применена вручную к
одному файлу). Практика подтвердила выбор: пресет `full` зафиксирован на 90 ходов
(коммит `bf0d19b`, 2026-08-14) с ручной калибровкой каждого production-model файла через
`Game.Balancing`; когда понадобилось другое число ходов (тимбилдинг, №17), сделали **отдельный** файл
с собственной ручной калибровкой, а не растянули таймер хода на готовый файл — хотя арифметика бы
сошлась (2 мин × 90 ходов = 180 мин). Число ходов и длительность хода остаются обычными явными
полями `SessionPresetConfig`, как и до этого пункта, без вывода одного из другого. Пункт закрыт как
осознанно отклонённый — не как выполненный и не как отложенный «на потом».

**Сложность:** Высокая — не только UI ввода параметров, а пересмотр, какие константы конфига
абсолютны, а какие обязаны масштабироваться от числа ходов; ошибиться здесь значит незаметно
разбалансировать все существующие пресеты разом.
**Важность:** Средняя — полезная гибкость для разных форматов мероприятия, но не блокирует уже
работающие фиксированные пресеты (debug/training/pilot).

### 13. Арена ботов для прогона сценариев

Автоматизированные прогоны нескольких ботов по сценарию (`Game.Bots`/`Game.Balancing` уже есть как
фундамент, Блоки 7.1-7.2 `BUILD_PLAN.md`) — для последующего анализа: балансировка цепочки
производства и других параметров конфига по результатам массовых прогонов, а не по одной ручной игре.

*Развито в сессии 2026-08-13* (`docs/balancing-bots.md`, детальный план перед реализацией): не один
ансамбль ботов, а сетка стратегий по двум осям — кредитное плечо (`leverage`) и распределение усилий
по времени (`profile`); нынешний `SimpleBot` — одна конкретная точка сетки, не переписывается заново.
Сравнение — не между партиями одного профиля (как сейчас `AverageFinalScoreSpread`), а между ячейками
сетки и относительно целевой функции `X(t)` идеального зала (`docs/production-balance.md`).

*Финализировано в сессии 2026-08-14* — план разбит на подблоки и запланирован как `BUILD_PLAN.md`
**Блок 7.3** («Идеальный зал X(t) и сетка ботовых стратегий»). Ключевые решения этой сессии, которых
не было в черновиках 13-го числа: (а) задача разовая/редкая — правки одного production-model файла, к
которым потом могут не возвращаться годами, поэтому обязательная часть блока — не код, а
`docs/balancing-tool-howto.md`, глоссарий «число в JSON → диагноз → какой параметр конфига крутить»
для человека без контекста; (б) утилита прогоняет **один** production-model файл целиком за вызов
(`--config`, все секторы файла в одной партии), не автопрогон по всей лестнице стадий сразу — рабочий
цикл ручной и последовательный, файл за файлом; (в) JSON-отчёт — только агрегированные числа (спред
между ветками, доля дефолтов/вынужденных ремонтов, тепловая карта сетки), без трасс отдельных действий
ботов; (г) прогоны сетки рассчитаны на часы без вмешательства — нужен heartbeat в консоль, не
оптимизация скорости.

*Реализовано в сессии 2026-08-14* — весь Блок 7.3 (7.3.1-7.3.7) закрыт: пробелы бота, сетка
leverage/profile, выбор конфига без хардкода, идеальный зал X(t), метрики сходимости, компактный
JSON-отчёт (все в коде, покрыты тестами — 820 тестов по солюшн) и, последним, `docs/balancing-tool-howto.md`
— инструкция запуска и глоссарий «число в JSON → диагноз → какой параметр конфига крутить» из пункта
(а) выше. Сама калибровка (прогон утилиты на всех 4 файлах лестницы стадий и правка их чисел по
результатам) этим пунктом не покрывается — план строил инструмент, не выполнял калибровку им.

**Сложность:** Низкая-средняя — фундамент уже есть, нужно в основном связать существующие куски в
повторяемый прогон + отчёт, не строить с нуля.
**Важность:** Средняя — не игрок-facing сам по себе, но напрямую ускоряет и обезопашивает пункты 5
(менее прибыльная экономика) и 2 (ветки технологий) — без него их калибровка идёт вручную и медленно.

### 18. `IdealHallCalculator` не моделирует износ фабрик *(предложено Claude)*

Найдено при калибровке `docs/difficulty.md` шаг 3 (сессия 2026-08-15, изолированный прогон рычага
`AccelerationFactorPerTurn`): X(t) идеального зала не менялся вообще ни при каком множителе скорости
износа. Причина — `IdealHallCalculator` нигде не обращается к `Factory.Condition`: `CalculateFactoryUpkeep`
вызывается с неявным Condition, всегда эквивалентным 1.0 (не изношено), капремонт/вынужденный простой
в модели идеального зала не наступают никогда. Это противоречит явному допущению `docs/production-balance.md`
§4 («капремонт заказывается вовремя, по первой уже осмысленной ступени `WearConfig.OverhaulTiers`, не
раньше и не позже») — на деле допущение сильнее: износа нет вовсе, а не «чинится идеально вовремя».

Практическое следствие: у любого рычага/фикса, завязанного на `WearConfig`, `X(t)` и метрика
`OverallAverageFinalConvergence` его не увидят — нужно смотреть `ForcedRepairEventShare` в сетке
ботов (и то только с `--maintain-factories false`, иначе аккуратные боты тоже не дают эффекту
проявиться, см. `docs/difficulty.md` §5 шаг 3).

**Сложность:** Средняя — нужно завести и обновлять `Condition` внутри модели идеального зала по ходу
расчёта X(t), плюс решить, в какой момент «эталонная политика» заказывает капремонт (сейчас в коде
для этого нет вообще никакой логики, не только неточной).
**Важность:** Низкая-средняя — не блокирует уже работающую калибровку остальных рычагов (5 из 8 у
слайдера сложности откалибровались нормально без этого); важно только для рычагов, специфично
завязанных на износ.

### 14. Довести до конца расследование подвисаний интерфейса *(предложено Claude)*

Открытое расследование ([[project_ui_freeze_investigation]]) — временная диагностика (клиентская
`diagnostics.js`/`reconnection.js`, серверная `EnterSyncRootTimed`) уже в `main`, ждём лог с реального
повторного зависания. Если причина подтвердится (contention на `Host.SyncRoot`, полный replay журнала
на каждый посекундный тик), нужно будет само исправление и снятие временной диагностики. Прямая
предпосылка для пункта 7 — «не бороться с интерфейсом с телефона» касается и подвисаний, не только
навигации.

**Сложность:** Неизвестно, зависит от находки — от простой правки кэширования до пересмотра модели
конкурентного доступа к `GameSessionHost.Session`.
**Важность:** Высокая — надёжность интерфейса во время реальной игры критична, уже минимум один раз
испортила пользовательский опыт (таймер вставал, вкладки не переключались).

**Как убирать диагностику, когда решим, что она больше не нужна** (коммиты `ad48048`, `dfd65e7`,
`fd2fd4d` — не трогать, пока пользователь явно не попросит):
- Удалить `src/Game.Web/wwwroot/js/diagnostics.js` и `src/Game.Web/wwwroot/js/reconnection.js`.
- `src/Game.Web/Components/App.razor` — убрать `autostart="false"` у `blazor.web.js` (вернуть
  автостарт) и обе строки `<script src="js/...">` на эти файлы.
- `src/Game.Web/GameSessionHost.cs` — убрать `EnterSyncRootTimed`/`LockScope`/`SlowLockThreshold`,
  если решение подтвердит, что contention не в этом (если проблему это и вскрыло — вместо удаления
  превратить в постоянный, но менее шумный лог, а не выкидывать совсем).
- `src/Game.Web/PhaseTimerBackgroundService.cs` и все 10 страниц-таймеров (`BigScreen`, `Team`,
  `Negotiate`, `SessionControl`, `Facilitator`, `Needs`, `AdminParticipants`, `Admin`, `AdminTeams`,
  `Operator`) — вернуть `using (Host.EnterSyncRootTimed(...))`/`lock (_host.SyncRoot)` на обычный
  `lock (Host.SyncRoot)` без обёртки.
- `src/Game.Web/Program.cs` — убрать стартовую строку `[diag] Диагностика подвисаний активна...`.
- Прогнать `dotnet test` по всему решению и живой прогон `/screen` + `/team` перед коммитом удаления.

### 20. LLM-боты вместо формульных *(предложено Claude)*

Идея пользователя (сессия 2026-08-15): отдельная консоль без веб-интерфейса, играющая через движок
напрямую (как `BotSessionRunner`, только вместо формул `SimpleBot` — вызов к LLM). На каждый ход
решений формируется промпт с историей, текущей картиной (финансы, склад, рынок, фабрики) и явной
осью страх/жадность как часть персоны/системного промпта (не число в формуле — в этом весь смысл, дать
модели рассуждать); ответ — команда, которую код разбирает и исполняет через уже существующие методы
`GameSession` (`TakeLoan`, `BuildFactory`, `SetWorkerCount`, `SetRndCommitment`,
`SetGenerationResearchCommitment`, `SetOverhaulRequested`, `RepayLoan`, `SellToSystem`, заявки в
`OrderBook`). Локальный инференс — LM Studio, с прицелом и на Ollama; оба говорят OpenAI-совместимым
`/v1/chat/completions`, так что нужен один клиент с разным base URL, а не два.

Не замена `SimpleBot` в калибровке ([[project_multi_recipe_factory_gap]], [[project_production_staging_plan]])
— `BalancingHarness`/`StrategyGridRunner`/регрессионные тесты держатся на детерминизме и скорости
(сотни прогонов за секунды), LLM-бот и недетерминирован, и на порядки медленнее (секунды на ход ×
ходы × команды). Ценность в другом: качественный плейтест — ощущается ли экономика вменяемо для
рассуждающего агента, и конкретно закрывает пробел [[project_multi_recipe_factory_gap]] — ни один
формульный бот сейчас не вызывает `Factory.SelectRecipe`, LLM-боту это доступно по конструкции.

Ключевые риски, разобранные в обсуждении: 1) контекст-окно не тянет полную историю 90 ходов — нужна
свёртка, сохраняющая причинность, тот же принцип, что и [[feedback_event_causality_and_traceability]],
только применённый к промпту, а не к постоянному логу; 2) ответ модели должен идти JSON-схемой
(`response_format`/`json_schema`, есть у LM Studio и Ollama), не свободным текстом — иначе парсинг
команд у локальных небольших моделей превращается в мучение; 3) нужен цикл
исполнить→ошибка домена→вернуть текст ошибки модели→повтор с потолком попыток, иначе один
галлюцинированный вызов подвешивает весь прогон; 4) сырые промпты/ответы стоит логировать отдельно
от доменного event log сессии (не смешивать), но рядом — для разбора «почему бот так решил».

Порядок реализации, если возьмёмся: (1) слой команда+JSON-схема+валидатор, обкатать на
фейковой LLM с прописанными ответами, чтобы доказать путь execute→validate→retry без реального
инференса; (2) подключить LM Studio; (3) добавить Ollama на тот же интерфейс; (4) промпт-инжиниринг
и стратегия свёртки истории — самая долгая часть; (5) опционально смешать LLM-боты и `SimpleBot` в
одной сессии для контраста.

**Шаг (1) сделан (2026-08-16):** новый проект `Game.Bots.Llm` (+ `Game.Bots.Llm.Tests`, 8 тестов) —
`BotCommand` (плоский JSON, не `oneOf` по `Kind`, — маленьким локальным моделям проще стабильно
вернуть один объект с необязательными полями, чем строго переключаемую по тегу схему),
`BotCommandSchema` (JSON Schema для будущего `response_format`), `BotCommandExecutor`
(диспетчеризация в `GameSession`, доменные исключения → текст для ретрая) и
`LlmBotDecisionLoop` (execute→validate→retry с потолком попыток, `BotDecisionLog` — сырые
ответы отдельно от event log сессии). Доказано на `ScriptedLlmClient` (без реального инференса):
успех с первой попытки, доменная ошибка → ретрай → успех, все попытки исчерпаны без исключения,
битый JSON и отсутствие обязательного поля обрабатываются как та же доменная ошибка, `Nop`
завершает ход штатно. Промпт/снапшот состояния и свёртка истории — намеренно не в этом шаге, см.
шаг (4) выше.

**Шаг (2) сделан (2026-08-16):** `LmStudioClient : ILlmClient` — OpenAI-совместимый
`/v1/chat/completions`, `response_format: json_schema` по `BotCommandSchema`. Живой прогон против
реального LM Studio (модель `google/gemma-4-12b`, 32k контекст) вскрыл два практических вывода,
важных для шага (4):
1. **`strict: true` в JSON-схеме вреден для маленьких моделей** — OpenAI/LM Studio требуют в этом
   режиме перечислить в `required` вообще все поля схемы, и модель, не умея оставить нерелевантное
   поле пустым, подставляет вместо `null` правдоподобный мусор (выдуманный `factoryId`, гигантские
   числа в `amount`/`count`, ломающие парсинг переполнением `int`). С `strict: false` и `required:
   ["kind"]` — ответы компактные и по делу. Реализовано именно так.
2. **`description` у полей JSON-схемы не долетает до модели** — LM Studio строит из схемы грамматику
   вывода (форма гарантирована), но не подмешивает текст `description` в то, что видит модель;
   несколько прогонов подряд путали `factoryId` (ULID уже построенной фабрики) с
   `factoryDefinitionId` (каталожный тип для постройки) несмотря на подробные описания в схеме —
   цикл execute→validate→retry корректно логировал ошибку парсинга и исчерпывал попытки без падения,
   но саму фабрику модель так и не построила. Проблема ушла только когда то же самое было сказано
   прямым текстом в системном промпте — подтверждает, что схема годится только для формы ответа,
   смысловые пояснения обязаны идти в текст промпта. Это прямое указание для промпт-инжиниринга на
   шаге (4), не решение задним числом здесь.

С разъяснением в промпте живой прогон прошёл полный путь за один вызов: gemma-4-12b построила
`iron-mine` с первой попытки, фабрика реально появилась в `GameSessionState` (~4 секунды на ход).

Ollama (шаг 3) сознательно пропущена по решению пользователя (2026-08-16) — не блокирует
дальнейшую работу, тот же `ILlmClient`, тот же клиент, только другой base URL, можно вернуться в
любой момент.

**Шаг (4) начат (2026-08-16):** первая версия промпт-инжиниринга и снапшота состояния —
`SystemPromptBuilder` (статика: роль, явная цель — расти по net worth, справочник команд,
персона), `BotStateSnapshotBuilder` (динамика: баланс/долг, свои фабрики с настоящими ULID,
склад, рыночные котировки сектора, свои контракты, рейтинг команд — текстом, не JSON, дешевле по
токенам), `BotTurnHistory` (собственные прошлые ходы бота с аннотациями, скользящее окно) и
`LlmBot` (оркестратор поверх `LlmBotDecisionLoop`: собирает промпты заново на каждый ход, копит
свою историю между вызовами). Полная свёртка экономической истории сессии под контекст-окно
(риск №1) в это не входит — сюда входит только собственная история решений бота, которая
компактна сама по себе.

Живой двухходовой прогон против LM Studio (gemma-4-12b) вскрыл ещё два вывода:
1. **Баг, исправлен**: `LlmBotDecisionLoop` для исхода `Nop` не сохранял разобранную команду
   (`LlmBotTurnResult.Command` был `null`), поэтому аннотация модели терялась — ровно то, что
   аннотации должны сохранять (доказано регрессионным тестом на фейковой LLM).
2. **Находка про gemma-4-12b, закрыта проверкой на другой модели**: без явной цели в системном
   промпте модель на пустой команде выбирает `nop` вместо очевидного `takeLoan` — добавление явной
   цели, прямое правило и температура 0.8 поведение не изменили; в аннотации модель верно называла
   нужное действие, но не выдавала его как команду.

**Проверка на более крупной модели (2026-08-16, тот же день, по просьбе пользователя):**
пользователь подключил `qwen3.8-27b-mlx` (reasoning-модель, крупнее и медленнее gemma-4-12b) —
разрыв «понимание против действия» не воспроизвёлся: с первого хода `takeLoan(500)`, дальше по
ходам суммы займов совпадали с реальным `buildCost` из состояния (не выдуманные) — значит модель
реально читает и использует данные, а не просто угадывает. **Вывод: разрыв был про возможности
конкретной модели, а не про формулировку промпта** — три варианта промпта его не лечили, смена
модели вылечила сразу. Заодно две новые находки по ходу проверки:
1. **Баг, исправлен**: с этой reasoning-моделью LM Studio иногда кладёт весь ответ (включая наш
   JSON) в поле `reasoning_content` сообщения, а `content` оставляет пустым — даже при
   `finish_reason: "stop"` (не обрезка по лимиту токенов). `LmStudioClient` теперь откатывается на
   `reasoning_content`, когда `content` пуст; "классические" модели (gemma-4-12b) это не задевает,
   у них `content` всегда заполнен. Регрессионный тест — на форме реального ответа.
2. **Пробел в снапшоте, закрыт**: без списка реальных `factoryDefinitionId` в состоянии модель на
   одном из ходов придумала `"IronMine"` вместо настоящего `"iron-mine"` (доменная ошибка, ретрай
   отработал штатно, но зря потратил попытку) — в системном промпте был только один пример строкой
   текста, не настоящий каталог. `BotStateSnapshotBuilder` теперь включает секцию «FACTORY TYPES IN
   YOUR SECTOR» с точными id, названиями, стоимостью постройки и статусом разблокировки по
   поколению — после этого модель дважды подряд запросила заём ровно на `buildCost` показанной
   фабрики, не на случайную сумму.

За два реальных хода (с расчётом между ними) до постройки фабрики не дошла — оба раза брала заём
вместо стройки, что можно списать на осторожную персону в тесте, а не на новый баг; дальше это не
гонялось намеренно (вопрос пользователя — «будет ли действовать» — уже получил однозначный ответ).

**Разреженная экономическая история по ходам добавлена (2026-08-16, тот же день, прямой запрос
пользователя):** до этого в промпте не было вообще никакой истории по ходам — только срез текущего
хода (`BotStateSnapshotBuilder`) и собственные решения бота (`BotTurnHistory`), без баланса/выпуска/
остатков за прошлые ходы, о чём пользователь спросил напрямую. Пользователь выбрал именно
разреженный ряд по ходам, не сводку одной строкой. Добавлен `BotHistorySeriesBuilder` —
переиспользует тот же `FactoryHistoryCalculator`, что уже строит графики на `/team` и большом
экране в Game.Web (данные были готовы, просто не подключены к боту): точка на каждый 5-й ход плюс
первый и текущий ход (независимо от длины сессии — 90 ходов с шагом 5 дают ~19 точек, не 90), четыре
секции — свой net worth по ходам, net worth всех команд («большой экран»), выпуск по каждой своей
фабрике, остаток по каждому материалу на складе. На реальном 13-ходовом прогоне без LLM — ~100
токенов на весь блок; для полной сессии ожидаемо несколько сотен, не проблема для бюджета 16-32к.
Полная свёртка с сохранением причинности между точками (что произошло МЕЖДУ ходами 5 и 10, а не
только на них) по-прежнему не решена — это упрощённый первый шаг риска №1, не его закрытие.

**Файл метрик по ходам добавлен (2026-08-16, тот же день, прямой запрос пользователя):**
`BotMetricsLog` — простейший CSV (бот, ход, время ответа мс, размер запроса в байтах, отвеченная
команда), одна строка на реальный ход (включая все попытки ретрая внутри него, если были). Отдельно
от `BotDecisionLog` (тот — сырые промпты/ответы каждой попытки, для разбора «почему», не для
статистики). `BotMetricsLog.Create(path)` — реальный файл с автосбросом на диск (длинный прогон
должен быть читаем «на лету»), заголовок не дублируется между перезапусками. Перцентили (p50/p85
времени ответа, p50/p85/макс и рост размера запроса) — не считаются в коде, это следующий шаг,
когда наберётся достаточно реальных данных.

**`LlmBotSessionRunner` добавлен (2026-08-16, тот же день):** асинхронный аналог
`Game.Bots.BotSessionRunner` (тот — для `SimpleBot`, синхронный) — до этого не было вообще никакого
способа довести LLM-сессию до конца, ходы прогонялись вручную в тестовом коде. Та же форма цикла
(Settlement→RunTick→AdvancePhase; Decision→боты решают по очереди→AdvancePhase), боты строго
последовательно, бот за ботом.

**Первый реальный прогон стадии 1 одним нейтральным ботом (2026-08-16, тот же день, прямой запрос
пользователя «прогнать одним нейтральным ботом игру на 1 стадии»):** конфиг —
`production-models/metallurgy.json` (один сектор, **26 типов фабрик** — против 5 в игрушечном
пилотном конфиге, которым были покрыты все тесты до сих пор) + `sessions/pilot.json`, пресет
«short» (используется вручную заданный `endTurn`, не обязательно в диапазоне пресета — это ок для
разового прогона). Модель — `qwen3.8-27b-mlx` (та же «умная, но медленная», что закрыла разрыв
понимание/действие в предыдущей проверке).

Первая попытка (12 ходов) упала на 7-м ходу необработанным исключением — тем же HTTP 400 от LM
Studio («token count exceeds context length»), что и раньше, только теперь из-за размера самого
каталога типов фабрик (26 против 5) промпт переполнил контекст-окно на несколько ходов раньше, чем
успело накопиться много истории. Отсюда — два фикса выше по файлу (ограничение каталога поколением
команды+1, устойчивость цикла к исключениям клиента).

Со фиксами повторный прогон **дошёл до конца** (12 ходов, не упал), но с честным и не блестящим
результатом — реальный файл метрик:
```
bot,turn,response_time_ms,request_size_bytes,command
Нейтрал,1,72185,7152,takeLoan(1000)
Нейтрал,2,113197,7183,nop
Нейтрал,3,100590,11497,nop
Нейтрал,4,381,14470,(no valid command — retries exhausted)
Нейтрал,5..12,~60-90,14521..14918,(no valid command — retries exhausted)
```
Ходы 1-3 отработали нормально (заём 1000, потом дважды `nop`) — реальные ответы модели, 72-113
секунд на ход (медленная модель, растущий промпт). С хода 4 — снова переполнение контекста: судя по
времени ответа (десятки **миллисекунд**, не секунд — LM Studio отклоняет запрос мгновенно, ещё до
генерации) и по тому, что `request_size_bytes` дальше растёт линейно (14.5-14.9 КБ, это ~3.5-4К
токенов — скромно, далеко не 16-32К) — похоже, дело не в абсолютном размере промпта, а в том, что у
загруженной в LM Studio `qwen3.8-27b-mlx` **настроено контекст-окно меньше, чем 16-32К** (то, что
пользователь называл для gemma-4-12b, не обязательно так же выставлено для этой модели). Это не
доказано (не проверялось напрямую через настройки LM Studio), но самое вероятное объяснение —
следующий шаг именно здесь, не в дальнейшем ужимании промпта вслепую.

Итог по вопросу «всё ли уже есть, или ещё рано»: **не рано** — весь механический путь (клиент,
цикл ретрая, промпт, снапшот, история, метрики, раннер) работает и был реально пройден целиком без
падений. Но для содержательного прогона стадии 1 не хватило одного: подтверждённого достаточного
контекст-окна у загруженной модели в LM Studio. За 3 содержательных хода бот успел взять заём и
дважды воздержаться от действий — фабрику построить не успел, дальше диагностировать «что он
достиг» на этом прогоне уже нечем.

**Переключение на стационарный сервер + консольный раннер + устойчивость к многочасовому прогону
(2026-08-16, тот же день, серия запросов пользователя):**

1. **Другой сервер.** `LmStudioClient.DefaultBaseUrl` теперь читает `LM_STUDIO_BASE_URL` из
   переменной окружения (default — прежний `localhost:1234`) — переключение между ноутбуком и
   стационарным ПК в сети без правки кода. Пользователь выставил на стационарном ПК контекст-окно
   262К токенов.
2. **`LlmBotSessionRunner` теперь останавливается досрочно** (`LlmBotSessionRunResult`,
   `maxConsecutiveExhaustedTurns`, по умолчанию 3) при N провалов подряд у одного бота — «нет
   смысла гнать ходы до конца», прямой запрос пользователя, ровно то, что случилось в прошлом
   прогоне (9 из 12 ходов подряд впустую).
3. **`EndTurn` убран из промпта.** Проверено по коду `Game.Web`: реальный игрок на `/team` видит
   только «Ход N, фаза X» (`Team.razor`) — ни точный ход окончания, ни даже диапазон пресета нигде
   не показываются (разыгрывается один раз при старте, секрет от команд по SPEC). Бот не должен
   знать больше настоящего игрока.
4. **Аннотации короче** — системный промпт просит держать `annotation` до 12 слов: она копится в
   историю бота на каждый будущий ход, многословность стоит токенов из хода в ход.
5. **Новый проект `Game.Bots.Llm.Console`** + `run-llm-bots-stage1.bat` — то, чего не хватало для
   реального автономного прогона: раньше ходы прогонялись только вручную в тестовом коде. Раннер
   грузит стадию 1, поднимает 2-3 бота (три готовые персоны: осторожная/нейтральная/агрессивная) на
   заданное число ходов (по умолчанию 90), с щедрым таймаутом на запрос (20 мин — реальные замеры
   на стационарном ПК показали разброс от ~4 до ~17 минут на один ход) и повышенным числом попыток
   на ход (6). `.bat` — для Windows: проверяет, что рядом лежит `ImProd.sln`, что есть `dotnet`,
   пингует LM Studio перед долгой сборкой, держит окно открытым в конце (`pause`).
6. **Аудит устойчивости по чек-листу пользователя выявил и закрыл три реальных пробела:**
   - `BotDecisionLog` раньше жил только в памяти и писался на диск один раз, в конце успешного
     прогона — при падении или убийстве процесса терялось всё, включая самую важную последнюю
     попытку. `BotDecisionLog.CreateFile(path)` теперь пишет каждую попытку на диск сразу же
     (тот же приём, что и `BotMetricsLog.Create`).
   - Записи `BotDecisionLog` не были помечены, какому боту/ходу принадлежат, и не хранили сам
     текст запроса — только ответ. Теперь `BotDecisionLogEntry` несёт `BotLabel`, `Turn`,
     `UserPrompt` (именно тот текст, что был отправлен на этой попытке, включая дописанный текст
     прошлой ошибки при ретрае) — многочасовой многоботовый файл теперь разбираем без внешней
     привязки по порядку/времени.
   - `BotMetricsLog` («файл с ходами», который пользователь хочет отдавать на анализ) содержал
     только тайминг/размер/команду, но не исход хода — добавлены `balance`, `debt`, `net_worth`,
     `factory_count` (состояние сразу после исполнения решения, до расчёта — мгновенные эффекты уже
     видны, отложенные появятся в строке следующего хода той же команды, см. doc-comment класса).
7. **Streaming ответа (`stream: true`, SSE) — прямой запрос пользователя** после того, как один ход
   на стационарном ПК занял почти 17 минут: раньше нечем было отличить «модель ещё думает» от
   «всё сломалось». `LmStudioClient` читает ответ построчно через `HttpCompletionOption.ResponseHeadersRead`,
   собирает `content`/`reasoning_content` по кускам, с двумя колбэками — `onToken` (растущий счётчик
   кусков ответа, консоль крутит его на экране через `\r`, не заваливая скроллбэк) и `onStalled`
   (сколько времени подряд нет новых кусков; предупреждение с порогом 60с, дальше не чаще раза в
   30с — модель может просто долго думать над одним трудным местом, не обязательно зависла).
   Живой прогон подтвердил: счётчик растёт в реальном времени, поток действительно потоковый.

Живые прогоны на стационарном ПК подтвердили работоспособность всей цепочки целиком — новая схема
метрик, потоковая запись решений с текстом запроса, скрытый `EndTurn`, streaming — на реальных
успешных ходах (`takeLoan`, за 3м44с — 16м40с в разных попытках). Полный 2-3-ботовый 90-ходовый
прогон ещё не запускался (это буквально то, что пользователь теперь может запустить сам через
`.bat` и оставить на ночь/дольше).

**Несколько действий за один ход + 5 новых команд (2026-08-16, тот же день):**

1. **Ход — больше не одна команда.** Прямой запрос пользователя: реальный игрок за одну фазу
   решений успевает построить, нанять, поднять R&D подряд — прежний дизайн «1 ход = 1 команда» был
   МЕНЕЕ дееспособен, чем живой игрок за тем же столом. `LlmBot.TakeTurnAsync` теперь зовёт LLM
   повторно внутри одного хода — каждый раз показывая новую секцию промпта «THIS TURN» (что уже
   решено В ЭТОМ ходу, отдельно от `BotTurnHistory` — той про прошлые ходы) — пока модель не
   ответит `nop` («готово на этот ход») или не сработает страховочный потолок
   (`maxActionsPerTurn`, по умолчанию 8, `LLM_BOT_MAX_ACTIONS_PER_TURN` в `.bat`) на случай, если
   модель не научится сама вовремя останавливаться. `BotDecisionLog`/`BotMetricsLog` теперь несут
   номер действия внутри хода, не только сам ход; `LlmBotSessionRunner`'ный circuit breaker считает
   «полностью провальным» только ход БЕЗ единого успешного действия — бот, взявший заём и только
   потом застрявший на второй команде, всё же продвинулся.
2. **5 новых простых односторонних команд** (запрос пользователя, контракты сознательно отложены —
   «пока гоняем концепцию на одной области», это требует отдельного дизайна двусторонней сверки
   заявок, не тривиальное добавление ещё одного `BotCommandKind`): `SellFactory` (ликвидация),
   `SetFactoryAllocationShare` (доля при дефиците сырья между своими фабриками), `PostNeed`/
   `WithdrawNeed` (доска потребностей), `EmergencyPurchase` (аварийная закупка). Бот теперь умеет
   14 команд из ~20 в `GameSession` — из оставшихся не-контрактных: `AdjustMarketPrice`/
   `PublishManualNews`/`GrantToTeam` и т.п. — операторские, не командные, боту не нужны.

**Отключение reasoning + анти-залипательные guard'ы + один вызов LLM на ход (2026-08-16, тот же день):**

1. **Reasoning выключен по умолчанию.** Прямой запрос пользователя: reasoning жрёт токены и сильно
   замедляет ход. Задокументированный для Qwen3 способ (`chat_template_kwargs: {enable_thinking:
   false}`) живьём проверен против этого же сервера и НЕ сработал — `reasoning_content` остался тем
   же. `reasoning_effort: "none"` сработал (пусто, 0 reasoning-токенов, и в потоковом режиме, и с
   `response_format`), также безвреден для не-reasoning моделей. `LmStudioClient` теперь принимает
   `disableThinking`, включено по умолчанию (`LLM_BOT_DISABLE_THINKING`, `.bat`).
2. **Два живых прогона (gemma-2-9b-it, затем qwen3.8-27b без reasoning) вскрыли залипание**: боты
   штамповали одну и ту же команду (`buildFactory`/`emergencyPurchase`) по 5-8 раз за ход подряд, не
   реагируя на изменившееся состояние — долг улетал в миллионы за несколько ходов. `emergencyPurchase`
   особенно опасна (steep markup) и один раз модель даже покупала материал для фабрики, которой у неё
   не было. Добавлены в `LlmBotDecisionLoop`: детектор буквального повтора команды в пределах хода
   (кроме `buildFactory` — несколько фабрик одного типа валидны) и жёсткий лимит «не больше одной
   `emergencyPurchase` одного материала за ход», который выжил даже когда модель начала обходить
   первый guard, слегка варьируя объём. `maxActionsPerTurn` снижен 8→5.
3. **Переход на один вызов LLM за весь ход** (прямой запрос пользователя: «только раз за ход
   обращаться к LLM, и чтобы он сразу формировал массив команд на ход» — не по одному действию на
   вызов). Ответ — `{"actions": [...]}` (`BotCommandBatch`), схема — `BotCommandSchema.BuildBatch`.
   `LlmBotDecisionLoop` разбирает и исполняет массив по порядку одним проходом; ретраи
   (`maxAttempts`) — только на уровне ВСЕГО ответа (битый JSON/сеть), доменная ошибка ОДНОГО действия
   внутри уже распарсенного массива больше не запускает повторный запрос с исправлением — действие
   просто пропускается (`LlmBotTurnOutcome.Skipped`, причина сохраняется в кросс-ходовую историю),
   остальные действия того же массива всё равно исполняются. Осознанное ограничение: команда,
   нацеленная на фабрику, построенную РАНЕЕ В ТОМ ЖЕ массиве, сработать не может — её id движок
   выдаёт только в момент постройки; проговорено в `SystemPromptBuilder`. Живая проверка (точный HTTP-
   запрос, который шлёт код) против LM Studio подтвердила: модель сама планирует `takeLoan` +
   `buildFactory` одним ответом за ~12с (раньше — два отдельных вызова, ~20-30с).
4. Попутно найден и исправлен независимый баг: `BotCommandSchema` годами перечисляла значения `kind`
   в PascalCase (`Enum.GetNames`), а разбирающий JSON-конвертер ждёт camelCase — вживую не било,
   потому что модель ориентировалась на camelCase-примеры в тексте промпта, а не на enum схемы.

**Первый полный 90-ходовый прогон после батч-переделки — подтверждено, подход работает
(2026-08-17):** запрос пользователя — разобрать результаты и завести архив прогонов, чтобы не
перечитывать логи с нуля каждый раз (прогон вычислительно тяжёлый, часы на 90 ходов). Заведён каталог
`docs/bot-runs/` (свой `README.md`-индекс + подробный `ANALYSIS.md` на каждый прогон, посчитанный
разбором логов, а не на глаз). Первая запись —
[`docs/bot-runs/2026-08-16-stage1-qwen3.8-27b/ANALYSIS.md`](bot-runs/2026-08-16-stage1-qwen3.8-27b/ANALYSIS.md):
3 бота × 90 ходов, qwen/qwen3.8-27b, reasoning выключен, ~5ч10мин. Итог: 90/90 ходов до конца, 99.8%
успешных действий (1129/1131), 0 срабатываний обоих анти-залипательных guard'ов (ни разу не
понадобились — модель сама не залипала), 0 `Nop`/`Exhausted`, все 3 бота выросли в netWorth без
сбоев. Замечено: 63% ходов упирались в `maxActionsPerTurn=5` — не залипанием, а легитимной продажей
выпуска у ботов с 10+ фабриками (стоит поднять потолок); ключевая исходная мотивация задачи
(`selectRecipe`) в этом прогоне не проверена — в пилотном конфиге сектора A у каждой фабрики ровно
один рецепт, выбирать не из чего; доска потребностей и `setFactoryAllocationShare` тоже не
использовались (3 бота — 3 изолированные копии одной экономики, реального обмена между ними не было).
Подробности, все цифры и раздел «что дальше» — в `ANALYSIS.md` по ссылке выше, не дублируются здесь.

**Потолок действий поднят 5→8 + попутный баг схемы (2026-08-17, тот же день):** прямое следствие
разбора выше — раз guard'ы ни разу не сработали, а 63% ходов упирались именно в потолок, подняли
`MaxActionsPerTurn` по умолчанию (`RunSettings`, `.bat`) с 5 до 8. Попутно найден и исправлен баг:
`LmStudioClient` всегда строил JSON-схему через `BotCommandSchema.BuildBatch()` без аргумента, то есть
`maxItems` в схеме, которую видит модель, был жёстко зашит в умолчание схемы (10) и никогда не совпадал
с тем, что реально принудительно обрезает `LlmBotDecisionLoop` — модель узнавала о настоящем потолке
только из текста системного промпта, не из самой схемы. `LmStudioClient` теперь принимает
`maxActionsPerTurn` в конструктор и строит схему тем же числом, каким его вызывает `Program.cs`
(`settings.MaxActionsPerTurn`) — оба места используют одно значение. Контекст-окно проверять не
пришлось: пользователь уже выставил на стационарном сервере 262К токенов (см. запись «Переключение на
стационарный сервер...» выше), а прогон из ANALYSIS.md вырос только до ~31 КБ (~8-10К токенов) на
самом крупном боте — запас на порядок; единственный реальный инцидент с переполнением контекста был
раньше, на другом (ноутбучном) сервере с гораздо меньшим выставленным окном, задокументирован там же.

**Стадия 1: выбор рецепта — от «никогда не проверялось» до реально работающего (2026-08-17, тот же
день):** прямое продолжение находки из предыдущей записи («ключевая исходная мотивация задачи —
`selectRecipe` — в прошлом прогоне не проверена, у каждой фабрики ровно один рецепт»).

1. **`SimpleBot`/`IdealHallCalculator` теперь строят фабрику НА КАЖДЫЙ рецепт многорецептного типа**
   (запрос пользователя: «научим бот строить все варианты фабрик с каждым рецептом — смысл тот же, как
   все фабрики построить»), а не одну с рецептом по умолчанию (`Recipes[0]`) — тот самый молчаливый
   выбор, из-за которого ни один формульный бот раньше не вызывал `GameSession.SelectRecipe`
   (`docs/production-staging.md`, «Стадия 4»: единственная попытка добавить многорецептную `mini-mill`
   в 2026-08-14 была откачена именно по этой причине). Единица достройки — пара (тип, рецепт), не
   тип: `BuildNewlyUnlockedFactories` в обоих местах перебирает `definition.Recipes`, разблокировка
   проверяется по `recipe.Output.Level` (не по `Recipes[0]`). Оказалось малой правкой — оба входа
   (`GameSession.BuildFactory`, `Team.BuildFactory`) уже принимали рецепт на постройке, отдельного
   вызова `SelectRecipe` не потребовалось. Риск для существующей калибровки — нулевой: ни в одном
   конфиге раньше не было многорецептной фабрики, значит новая ветка нигде не срабатывала до этой же
   правки; полный прогон тестов (938, весь `ImProd.sln`) подтвердил — 0 регрессий.
2. **Первая реальная многорецептная фабрика — `flex-reprocessing-shop` в `metallurgy.json`** (решение
   пользователя: один общий конфиг, не отдельный тестовый файл — «не хочется делать разные конфиги,
   дорого поддерживать и результаты не сопоставить»). Два новых рецепта, каждый ест то же сырьё, что
   уже потребляет существующая однорецептная фабрика (`scrap-alloy-from-scrap` — тот же вход, что и
   `mini-mill`; `coal-briquette-from-coal` — тот же вход, что и `coking-plant`), но с `ProductionRate`
   ×1.1 (реальный, посчитанный стимул выбирать) и НОВЫМ выходным материалом (`scrap-alloy`,
   `carbon-briquette`) — SPEC §5.2 («каждый материал ровно один производитель») буквально запрещает
   двум рецептам делить материал, `GameConfigValidator` упал бы на загрузке, если бы выходы совпадали
   с `wire-rod`/`coke`. Оба новых материала — чистые продавай-системе листья (не участвуют в цепочке
   дальше), маржа по уровню передела берётся из общей `MarginMultiplierByProcessingLevel`, отдельной
   калибровки не требуют. Побочный эффект — первый в конфиге сценарий реальной конкуренции за
   дефицитное сырьё внутри своей же команды (`flex-reprocessing-shop` соперничает с `mini-mill`/
   `coking-plant` за один и тот же вход), то есть и первый повод модели воспользоваться ещё одной
   ни разу не сработавшей командой — `SetFactoryAllocationShare`.
3. **Тесты**: `MultiRecipeFactoryTests` (`Game.Bots.Tests`) — `SimpleBot` строит обе комбинации (тип,
   рецепт) на маленьком самодостаточном фикстур-конфиге (не трогает общий `gameconfig.pilot.json`,
   на котором держатся десятки других тестов), идемпотентность повторного вызова тоже проверена.
   `MultiRecipeIdealHallTests` (`Game.Engine.Tests`) — раз публичный API `IdealHallCalculator.Calculate`
   не раскрывает список построенных фабрик ветки, проверка косвенная: конфиг с двумя рецептами даёт
   заметно (реально ~28%) бóльшую итоговую ценность ветки, чем тот же конфиг с одним — было бы 0%
   разницы, если бы второй рецепт по-прежнему молча игнорировался. `FlexReprocessingShopTests`
   (`Game.Bots.Llm.Tests`) — сквозная проверка на РЕАЛЬНОМ `metallurgy.json` (не игрушечном фикстур-
   конфиге): `buildFactory` с явным `recipeId` строит нужный вариант, без `recipeId` — берёт первый
   рецепт по умолчанию, `selectRecipe` переключает уже построенную фабрику, `BotStateSnapshotBuilder`
   реально показывает модели оба рецепта каталога — дешёвая страховка перед живым прогоном.
4. **`control-twin-metallurgy.json` синхронизирован тем же днём** (запрос пользователя: «синхронизируй
   сразу и зеркало, надо будет в ближайшее время ботов там тоже прогнать») — `flex-reprocessing-shop`/
   `scrap-alloy-from-scrap`/`coal-briquette-from-coal`/оба новых материала добавлены в ОБА зеркальных
   сектора (A и B, вторые — с суффиксом `-2`, тем же соглашением, что и весь остальной файл), без
   намеренных межсекторных перекрёстных связей — те три (`pig-iron-from-ore`+`coke-2` и т.п.)
   специально заведены раньше как единственная симметричная точка обмена между ветками и не
   расширялись. `SectorSymmetryRegressionTests` (обе проверки: `IdealHall`-траектории A/B побайтово
   совпадают, разброс конвергенции ботов между ними 0) прошли на обновлённом файле без изменений —
   подтверждение, что зеркало осталось честным зеркалом. LLM-боту в `Program.cs` менять
   ничего не пришлось — он и так грузит `metallurgy.json`, `selectRecipe`/`recipeId` у `buildFactory`
   были готовы в схеме/промпте/исполнителе с самого начала (шаг 1 плана); первая живая проверка того,
   пользуется ли модель этим на практике, — ещё не проводилась, это следующий прогон.

**Готовые производные показатели с трендом (2026-08-17, тот же день, прямой запрос пользователя):**
«маленькие модели плохо считают, но могут легче рассуждать над готовыми цифрами» — новый
`BotDerivedMetricsBuilder`, третий блок user-промпта поверх `BotStateSnapshotBuilder` (сырой срез) и
`BotHistorySeriesBuilder` (сырой ряд по ходам): сравнивает последнее N-ходовое окно (по умолчанию 5,
то же число, что и шаг разреженной истории) с таким же по длине предыдущим и явно называет тренд
словом (rising/falling/stable), а не отдаёт числа на откуп модели. Восемь показателей, все — из уже
существующих в движке calculators, ничего не выдумано заново: проценты и тело кредита раздельно
(`FinanceHistoryCalculator.OperationType.InterestCharged`/`MandatoryRepayment`/`VoluntaryRepayment` —
разделены на уровне событий движка изначально, не приближение), денежный поток (та же бухгалтерская
книга, доход минус расход по окну), плата за перегруз склада, простаивающие/недогруженные фабрики с
причиной (`Workers==0` → нет рабочих; `IsUnderRepair` → капремонт; `FactoryProduced.OutputQuantity` <
`CapacityLimitedOutputQuantity` на 5%+ → нехватка сырья — то самое разделение, ради которого поле
`CapacityLimitedOutputQuantity` изначально и заведено в событии), загрузка фабрик в % с трендом, total
R&D (сумма `RndCommitmentPerTurn`/`RndInvestment` по фабрикам — агрегата раньше не было нигде),
запас хода (сколько ходов до нуля баланса при текущем среднем денежном потоке, если он отрицательный),
рыночная позиция (% net worth от лидера, приближается/отстаёт). Пользователь сам поправил
формулировку «потери на складе» на «плата за перегруз» — в движке нет и никогда не было физической
порчи/переполнения материала (SPEC), только тариф сверх бесплатного лимита; выдумывать несуществующую
механику не стали. Системный промпт (`SystemPromptBuilder`) явно просит модель доверять этому блоку
и не пересчитывать тренды заново по сырой истории. 8 новых тестов (`BotDerivedMetricsBuilderTests`),
включая двухкомандную сессию (`TestSession.StartTwoTeamSession`, впервые в этом тестовом проекте) для
проверки рыночной позиции против реального конкурента, а не самой себя. 102/102 теста, полная сборка
решения зелёная. Живой прогон против LM Studio с этим блоком в промпте — ещё не проводился.

**Межсекторная торговля ботов: доска публичных заявок (2026-08-17, прямой запрос пользователя):**
запрос — «межсекторная торговля для ботов». Первая идея (переиспользовать `Game.Bots.OrderBook` —
механический стакан, уже рабочий для `SimpleBot`) пользователь отклонил: `LimitPrice`-фильтр этого
стакана — техническая абстракция, придуманная только для ботов, реальные игроки её не видят и не
понимают (проверено по коду `Game.Web`: `/team/needs` показывает грубую доску без цифр и цены,
`/team/negotiate` — только свои данные + форму для бланка сделки; сама цена и объём согласуются
устно, вне системы). Пользователь предложил свой отдельный механизм, специально под ботов, раз
переговоров у них нет: твёрдая публичная заявка «продаю/покупаю X, столько-то за раз (или каждый
ход), цена в диапазоне» — диапазон вместо торга, живёт не больше 3 ходов, снимается, как только
кто-то говорит «исполняю».

Реализовано как настоящий доменный механизм, не только слой бота — по образцу уже существующей
доски потребностей (`NeedPosting`/`NeedPosted`/`NeedWithdrawn`), а не в орбите
`Game.Bots.Llm`/оркестратора, потому что живёт по несколько ходов и должна быть частью
воспроизводимого журнала сессии, а не памяти консольного процесса:
- `TradeOffer` (`Game.Domain`) — заявка: кто, продаёт или покупает, материал, разовая/регулярная
  поставка, объём, диапазон цены, ход публикации; `TradeOffer.MaxAgeInTurns = 3` — константа в коде,
  не конфиг-настройка (точное число из запроса, конфигурируемость не просили). `IsOpenOn(currentTurn)` —
  чисто вычислительная проверка срока годности, без отдельного события «просрочено» и без нового шага
  в `RunTick`: не нужно мутировать состояние ради того, что и так посчитать один раз при чтении.
- `GameSession.PostTradeOffer`/`WithdrawTradeOffer`/`MarkTradeOfferFulfilled` — три метода, каждый
  пишет ровно одно событие (тот же принцип, что и везде в движке), в отличие от `PostNeed` — с
  гейтом `EnsureDecisionsAllowed()`: заявка не слух, а твёрдое намерение, из которого при исполнении
  выходит настоящий контракт.
- Само сведение и подтверждение контракта на условиях заявки — **не** внутри `GameSession` (там
  получился бы первый метод, пишущий больше одного события), а в вызывающем коде
  (`BotCommandExecutor.ExecuteFulfillTradeOffer`), тем же приёмом, что уже доказан
  `Game.Bots.OrderBook.SignContract` для стакана `SimpleBot`: собрать пару совпадающих
  `ContractProposal` из условий заявки → `SubmitContractProposals` → `ConfirmContract` (сторона,
  исполняющая чужую заявку, не может подтвердить сама себе — эта роль всегда у автора заявки, он уже
  выразил согласие публикацией) → `MarkTradeOfferFulfilled`. Значит, никакого специального
  ии-переговорного протокола между двумя ботами не понадобилось вообще — то, что раньше считалось
  «нетривиальным дизайном двусторонней сверки заявок» и было отложено, здесь просто не встаёт: вторая
  сторона уже согласилась самим фактом публикации заявки, а не отдельным independent-заявочным актом.
- 4 новые команды бота: `postSellOffer`/`postBuyOffer` (materialId, volume, minPrice, maxPrice,
  recurring?) и `withdrawTradeOffer`/`fulfillTradeOffer` (tradeOfferId, + volume/unitPrice для
  исполнения) — раздельные глаголы на продажу/покупку, тем же стилем, что `takeLoan`/`repayLoan`, не
  один параметризованный командой направления.
- **Видимость** — прямое исправление находки по ходу расследования: `BotStateSnapshotBuilder` до
  этого вообще не показывал доску потребностей ни одному боту (`PostNeed` только писал, никто не
  читал) — не тронул тот пробел отдельно, но по аналогии добавил новую секцию «PUBLIC TRADE OFFERS»
  сразу видимой для всех: только реально ещё исполнимые заявки (`IsOpenOn`), с пометкой `(you)` у
  собственных. Системный промпт (`SystemPromptBuilder`) объясняет диапазон цены как замену
  недоступному торгу.
- Требовалась протяжка общего `Random` (для кода подтверждения контракта, тот же генератор, что и у
  `GameSession.RunTick`, ради воспроизводимости журнала при одном сиде) через
  `BotCommandExecutor.Execute` → `LlmBotDecisionLoop.RunTurnAsync` → `LlmBot.TakeTurnAsync` →
  `LlmBotSessionRunner` — механическая, но сквозная правка сигнатур, задела все существующие тесты
  этого слоя.

22 новых теста (`GameSessionTradeOffersTests` — доменный уровень: гейт фазы, просрочка через 3 хода,
самоисполнение запрещено, повторное исполнение запрещено; `BotCommandExecutorTests` — уровень бота:
публикация, отзыв, исполнение с реальным контрактом, цена вне диапазона, попытка исполнить свою же
заявку; `BotStateSnapshotBuilderTests` — видимость). Полная сборка решения и все 969 тестов (было
947) зелёные. Контракты как таковые (полноценные переговоры с обеих сторон, ручной ввод условий,
подтверждение оператором) по-прежнему не тронуты и не нужны ботам — этот механизм их не заменяет, а
даёт более простой путь специально для автоматических агентов. Живой прогон против LM Studio с новыми
командами — ещё не проводился.

**Промпт явно подсвечивает износ фабрик (2026-08-19, прямой запрос пользователя, по следам разбора
прогона — см. пункт 21 и `docs/bot-runs/2026-08-19-stage1-gpt-oss-20b/ANALYSIS.md`):** бот в том
прогоне ни разу за 90 ходов не вызвал `setOverhaulRequested` — данные для этого были в срезе состояния
и раньше (`condition=X%` у каждой фабрики), но ничто не подсказывало модели, что с этим надо
что-то делать, пока не стало поздно. Три изменения:
1. `BotStateSnapshotBuilder` — у каждой фабрики теперь виден флаг `overhaulRequested=true/false`, не
   только `condition`/`status` — раньше бот не мог отличить «уже попросил капремонт» от «ещё нет» без
   собственной памяти по ходам.
2. Новая секция `BotDerivedMetricsBuilder`: **«FACTORY WEAR — ACT BEFORE IT'S FORCED»** — явно
   перечисляет фабрики с `condition < 2×CriticalConditionThreshold` (порог не выдуман, читается из
   `Wear`-конфига сессии), у которых капремонт ещё не запрошен и которые ещё не в вынужденном
   простое — то есть «вот-вот сломается», отдельно от уже существующей «IDLE / UNDERPERFORMING
   FACTORIES» (та — про то, что УЖЕ сломалось).
3. `SystemPromptBuilder` — описание `setOverhaulRequested` теперь прямо объясняет экономику решения:
   запросить рано (тариф «Профилактика») — дёшево и 1 ход простоя; тянуть до принудительного —
   `ForcedRepairDurationTurns` (до 8) ходов простоя без спроса.

4 новых теста, включая калиброванный по реальной формуле износа сценарий (`BotDerivedMetricsBuilderTests`:
фабрика, оставленная деградировать 23 хода без капремонта, попадает в предупреждение; со свежим
капремонтом — нет). **Не проверено живым прогоном** — эффект увидим только на следующем реальном
запуске бота с этими изменениями; заметки для себя на будущее — в `_bots_llm/NOTES.md` (не в git,
рабочий каталог пользователя).

**Второй прогон подтвердил wear-фикс + подсказка про диверсификацию + обязательное `reason`
(2026-08-19, тот же день, по итогам разбора второго прогона):** прогон v2 (тем же сетапом, сразу
после фиксов выше) дошёл до net worth +3.3 млн без единого обвала, с 13 проактивными
`setOverhaulRequested` — подтверждение, что подсказка про износ реально меняет поведение модели. По
итогам разбора v2 пользователь задал 5 вопросов об устройстве раннера (детально отвечено в
`docs/bot-runs/2026-08-19-stage1-gpt-oss-20b-v2/ANALYSIS.md`) и попросил три доработки:
1. **Подсказка про диверсификацию** — в `SystemPromptBuilder` добавлено явное правило: если рынок
   насыщен (продаёшь у потолка ёмкости несколько ходов подряд, цена не растёт/уже упала и стоит),
   строить ЕЩЁ того же типа бессмысленно — посмотри другие типы в каталоге. Прямой ответ на находку
   v2 (7 фабрик одного профиля при профиците капитала в миллионы).
2. **Новое обязательное поле `reason`** — «объясни, почему именно это действие сейчас», в отличие от
   необязательной `annotation` НЕ уходит в `BotTurnHistory` и не растит промпт из хода в ход
   (осознанно ephemeral — только в `BotDecisionLog`, для разбора человеком). Схема требует его для
   каждой команды (`required: ["kind", "reason"]`), `annotation` осталась опциональной ровно как
   была — две разные роли: `reason` объясняет решение здесь и сейчас, `annotation` — памятка себе на
   будущее.
3. **Обратная связь по неудавшимся действиям на будущий ход — уже была реализована** (проверено на
   реальных данных прогона v2, не нужно строить заново): `LlmBotTurnResult.ForSkipped` кладёт текст
   доменной ошибки в кросс-ходовую историю через `BotCommandSummary.Describe`, и в самом прогоне v2
   бот на ходу 50 забыл `enabled` у `setOverhaulRequested`, увидел на ходу 51 в своей истории «...
   requires factoryId and enabled» и тут же исправился, вызвав команду правильно.

8 новых/обновлённых тестов (обязательность `reason` в схеме, отсутствие утечки `reason` в историю).
977/977 тестов, сборка чистая. Не проверено живым прогоном.

**Четвёртый пункт запроса пользователя — вести с моделью короткий "диалог" внутри хода (показывать
реальный ответ движка после каждого действия, а не молча пакетом) — обсуждён, не реализован.** Это
по сути возврат к домартовной архитектуре «один вызов LLM на действие», от которой отказались
26.08.16 именно потому, что модель залипала (штамповала одну и ту же команду 5-8 раз подряд), а не
только из-за задержки. `openai/gpt-oss-20b` на порядок быстрее qwen3.8-27b (секунды, не минуты на
вызов), так что довод «слишком дорого по времени» сейчас слабее, чем был — но довод «показывает ли
это реальный ответ движка» уже частично не в счёт: в старой архитектуре модель и так видела
актуальный срез состояния на каждом вызове (в т.ч. факт неудачи предыдущего действия), просто не в
виде текста «вот ответ движка» — и всё равно залипала. Главный настоящий выигрыш такого дизайна не в
«разговорности», а в снятии текущего явного ограничения батч-режима: построить фабрику и тут же
нанять на неё рабочих в ОДНОМ ходу сейчас невозможно (движок не знает id до постройки). Решение —
за пользователем, не бросился делать в одностороннем порядке.

**Прерывание/возобновление прогона (2026-08-19, тот же день, прямой запрос пользователя):**
«прервать Ctrl+C, потом запустить — и продолжит с прерванного места». Разведка нашла, что нужная
инфраструктура для игрового состояния уже есть и уже проверена в бою —
`Game.Persistence.DurableEventLog<TState>`, та же durable-обёртка над журналом, которой `Game.Web`
уже пользуется для восстановления сессии после сбоя: пишет каждое событие на диск сразу при
`Append`, `Open(journalPath, snapshotPath, createInitialState)` одним вызовом обслуживает и старт с
нуля (файлов ещё нет), и восстановление (снапшот + доигрывание хвоста журнала). `GameSession` уже
умеет обернуть такой журнал напрямую (`new GameSession(durableLog)` и перегрузка
`StartWithEndTurn(IEventLog<GameSessionState>, ...)` — обе уже существовали, просто не были нужны
консольному раннеру раньше). `BotMetricsLog.Create`/`BotDecisionLog.CreateFile` и без того дозаписывают
существующий файл без дублирования заголовка — задел на это был сделан ещё в рамках аудита
устойчивости 16 августа.

Новое — `BotRunCheckpoint` (`.working.json` рядом с exe, по запросу пользователя «сделай там какие
нужно поля, каждый ход просто обновляй файл»): то, что журналом не покрывается — пути файлов этого
прогона (чтобы дозаписывать те же самые, не плодить новые с новой меткой времени), сид `Random`
(зафиксирован в момент старта, не «на лету» — возобновление получает воспроизводимый, но не
идентичный прерванному прогону поток чисел вперёд от точки возобновления, бит-в-бит тут не нужен) и
собственная память каждого бота (`BotTurnHistory` — аннотации о прошлых ходах, не часть игрового
журнала, иначе бот проснулся бы с чистой памятью о своих же рассуждениях). Переписывается целиком
после каждого хода (`onTurnCompleted`), атомарно (временный файл + `File.Move`, тот же приём, что
уже был в `SnapshotFile`) — Ctrl+C посреди записи не может оставить битый файл. Удаляется по чистой
остановке (оба исхода — `Completed` и `RepeatedFailures`, оба осознанные, не сбой); при необработанном
исключении внутри прогона — сознательно НЕ удаляется, чтобы можно было попробовать продолжить.

Разбирался отдельно вопрос прерывания ПОСРЕДИ хода (один бот из нескольких уже походил в рамках
фазы решений, другой — ещё нет): специальной логики «пропустить уже походивших» решил не городить —
движок и так разрешает команде подать больше decision-действий до `AdvancePhase`, так что бот,
которого раннер на всякий случай спросит снова, просто получит ещё одну возможность действовать в
рамках того же хода (не баг, максимум небольшой бонус, не порча состояния).

7 новых тестов на восстановление памяти бота (`BotTurnHistory`/`LlmBot` теперь принимают
`initialEntries`/`initialHistory` в конструкторе) — низкоуровневый механизм `DurableEventLog` и без
того обстоятельно покрыт существующими тестами `Game.Persistence.Tests` (restart-и-продолжить-цепочку,
восстановление со снапшотом и без, порча журнала). Отдельный сквозной тест на реальных `GameSession`/
`Change<GameSessionState>` через этот же рецепт не заводил — сочтено избыточным поверх уже
существующего покрытия обоих слоёв по отдельности; `BotRunCheckpoint`'а JSON-сериализация тоже не
протестирована отдельным юнит-тестом (простой record + тот же атомарный приём записи, что уже
проверен на `SnapshotFile`) — оба этих места стоит перепроверить, если реальный прогон с
возобновлением покажет проблему. 980/980 тестов, полная сборка решения зелёная. **Не проверено живым
прогоном с реальной LM Studio** — здесь такого сервера нет, первая настоящая проверка — на машине
пользователя.

**Сложность:** Высокая — новый консольный проект, слой валидации команд, интеграция с двумя
локальными LLM-бэкендами, и главное — промпт-инжиниринг и свёртка истории под контекст-окно, у этого
нет готового рецепта в проекте.
**Важность:** Средняя — не блокирует пилот и калибровку (для них остаётся `SimpleBot`), но
потенциально ценный качественный инструмент проверки экономики и единственный реалистичный путь
закрыть [[project_multi_recipe_factory_gap]] автоматизированно, не руками через UI.

**Стадия 2 — межсекторная торговля (2026-08-20, запрос пользователя):** прежде чем гонять доску
публичных заявок, сперва прогнать один бот на стадии 1 как регрессию (потолок ставки 0.1, предупреждение
про износ, обязательный `reason`, чекпойнт/резюме — ни одно ещё не проверено живым прогоном). Затем
`run-llm-bots-stage2.sh` — та же консоль, два бота, `metallurgy-petrochemistry.json`
(`docs/production-staging.md`, стадия 2), по одному боту на сектор A/Б. `RunSettings`/`Program.cs`
раньше жёстко указывали `metallurgy.json` и сектор `"A"` на всех ботов — теперь оба параметра берутся
из окружения (`LLM_BOT_PRODUCTION_MODEL`, `LLM_BOT_SECTORS`, список через запятую, боты разбираются по
кругу как персоны), стадия 1 не тронута по поведению (её `.sh` просто не выставляет эти переменные,
остаются умолчания). `run-llm-bots-stage1.bat` удалён (пользователь: «bat файл удали») — до этого он был рабочим
Windows-эквивалентом `.sh` (обновлялся синхронно, в т.ч. под чекпойнт/резюме), но реальные прогоны
сейчас идут только с двух macOS-машин, второй платформы раннер больше не держит. **Не проверено живым
прогоном** — первая проверка на машине пользователя.

**Первый прогон стадии 2 (v1, gpt-oss-20b) — доска работает, сделок ноль (2026-08-20, тот же день):**
оба бота прошли все 90 ходов чисто (0 крахов), но за весь прогон — 3 `postSellOffer`, 0
`fulfillTradeOffer`/`postBuyOffer`. Разобрал по конфигу: оба выставленных материала (`pig-iron` у А,
`pvc-resin` у Б) потребляются ТОЛЬКО внутри своего же сектора — ни один рецепт соседа их не ест
физически, сделка была невозможна в принципе. `reason` подтвердил: оба бота explicitly описали это
как «сбросить свой излишек» (конкурент `sellToSystem`), ни слова о том, что нужно соседу. Заодно
выяснилось, что ни один бот не достроил свой флагман за 90 ходов (А — максимум `blast-furnace`/
`fastener-plant`; Б — `boat-assembly-plant` только на ходу 86, `tire-carcass-plant` на ходу 90,
последнем) — при нулевой межсекторной торговле собрать финальный продукт (нужны и `engine`/`fasteners`
от А, и своя часть от Б) физически было нечем.

**Фикс (тот же день, прямой запрос пользователя):** мы владеем полным графом рецептов — считаем
межсекторный спрос программно, не полагаемся на то, что модель сама догадается.
`BotStateSnapshotBuilder.AppendCrossSectorDemand` — новая секция «CROSS-SECTOR DEMAND»: материалы
СВОЕГО сектора, которые ест хоть один рецепт ЧУЖОГО сектора (кандидаты на `postSellOffer`), и материалы
чужих секторов, которые ест хоть один СВОЙ рецепт (кандидаты на `postBuyOffer`/на что смотреть на
доске) — считается из `state.Config.FactoryDefinitions`/`Recipes` напрямую, работает для любого числа
секторов (2-4), не хардкод под стадию 2. Показывается только когда реально ЗАНЯТО больше одного
сектора (по командам сессии, не по каталогу конфига — тестовый `gameconfig.pilot.json` объявляет оба
сектора A/Б даже в однокомандных тестах). `SystemPromptBuilder.Build` — новый параметр
`hasMultipleSectors`: при нескольких занятых секторах добавляет абзац «CROSS-SECTOR TRADE»,
явно понижающий `sellToSystem` до запасного варианта и указывающий сперва проверить доску/новую
секцию. `LlmBot.TakeTurnAsync` сам решает `hasMultipleSectors` по факту занятых секторов в сессии — не
нужно прокидывать флаг через конструктор/раннер. 6 новых тестов (два сценария снапшота — фолбэк без
межсекторной зависимости на реальном `gameconfig.pilot.json`, и реальный кросс-сектор на отдельном
минимальном конфиге, который называет ровно нужные материалы; два на промпт; один сквозной на
`LlmBot`), `ScriptedLlmClient` заодно научился запоминать и системные промпты, не только
пользовательские. 986/986 тестов по всему решению, зелёное. **Не проверено живым прогоном** — первый
кандидат на проверку следующим запуском `run-llm-bots-stage2.sh`.

---

### 21. Ставка по займу не защищена от абсурдных величин при большом долге — потолок 0.5 недостаточен *(предложено Claude)*

Найдено разбором реального прогона LLM-бота, не руками
([`docs/bot-runs/2026-08-19-stage1-gpt-oss-20b/ANALYSIS.md`](bot-runs/2026-08-19-stage1-gpt-oss-20b/ANALYSIS.md)):
бот (openai/gpt-oss-20b) весь прогон ни разу не запросил капремонт (`setOverhaulRequested`), к ходу 60
несколько фабрик встали на вынужденный простой, бот дважды подряд ударил аварийными закупками одного
материала — баланс пробило, включился принудительный заём, долг скачком ушёл с 438 до 517 086. Это
реальная ошибка бота (или живого игрока с теми же привычками) — но то, что случилось ДАЛЬШЕ, уже не
объясняется поведением бота: за следующие 30 ходов net worth улетел в −4.6 триллиона, при том что сам
долг к концу — «всего» 559 миллионов, на три порядка меньше итоговой дыры в балансе.

Причина — `FinanceCalculator.CalculateEffectiveLoanRate` (`sessions/*.json`,
`LoanInterestRateGrowthPerUnitBorrowed`): ставка растёт линейно от АБСОЛЮТНОЙ суммы долга в деньгах,
без верхнего предела, а сами проценты — `Debt × rate`, то есть квадратично от долга. Уже на
официальном потолке из того же конфига (`MaxTotalDebt = 10 000 000`) формула давала ставку +3000
процентных пунктов — то есть сам «безопасный предел», который конфиг называет разумным, формулой не
выдерживался.

**Исправлено (2026-08-19, прямой запрос пользователя «почини формулу ставки по займу»):** новое поле
`StartingConditionsConfig.MaxLoanInterestRate` (0.5 = 50%/ход, во всех 4 файлах конфига — единый
пилотный + 3 сессионных), `FinanceCalculator.CalculateEffectiveLoanRate` клэмпит итоговую ставку
сверху им же — сколько бы команда ни назанимала, потери за один ход остаются конечной, осмысленной
величиной. `MaxTotalDebt` сознательно НЕ тронут: перечитав его doc-comment внимательнее, выяснилось,
что «добровольный `TakeLoan` этим не ограничен» — не забытая проверка, а прямо документированное
решение («решение и риск команды»), поэтому вторая часть находки (потолок долга не защищает от
добровольных займов) закрыта не блокировкой займов, а именно потолком ставки — команда по-прежнему
вправе назанимать сколько угодно, но математика перестаёт идти вразнос.

**Заодно — видимость для живых игроков (тот же запрос: «заметят ли реальные игроки, не поздно ли, надо
ли подсвечивать в интерфейсе»).** `Team.razor`: ставка по займу подсвечивается красным (`text-danger`,
тот же приём, что уже был у отрицательного баланса), когда она уже ≥ половины `MaxLoanInterestRate` — и
в сводке «Что у меня плохо», и во вкладке «Финансы» (там же текстовое предупреждение «гасите долг, а не
занимайте больше»). До этого ставка была просто ещё одной цифрой в тексте, без сигнала, что она уже
опасна — техническая видимость (число на экране) была, поведенческая (заметит ли команда вовремя) нет.

3 новых теста на сам потолок ставки (`FinanceCalculatorTests`) + обновлены все места, создающие
`StartingConditionsConfig` (новое обязательное поле). 976/976 тестов, полная сборка решения зелёная.

**Потолок 0.5 оказался недостаточным (2026-08-19, тот же день, разбор ещё одного прогона —
[`docs/bot-runs/2026-08-19-stage1-qwen38-v5-crash/ANALYSIS.md`](bot-runs/2026-08-19-stage1-qwen38-v5-crash/ANALYSIS.md)):**
qwen3.8-27b обвалилась в net worth −35 млн всего за 27 ходов — быстрее первой катастрофы, хотя
потолок ставки уже стоял. Причина ровно в том, что потолок сделал ставку конечной, но не щадящей:
50% в ход на долге, упёршемся в `MaxTotalDebt` (10 000 000), — это **5 000 000 процентов за один
ход**, математически непреодолимо для любой команды такого размера. Прямое подтверждение из блока
«DERIVED METRICS»: проценты за одно 5-ходовое окно — 4 024 402, плата за перегруз склада за то же
окно — 86 389 (на два порядка меньше, проценты — вся история). Триггер на этот раз — новый,
отдельный вид зацикленности бота: 10 ходов подряд путал собственный ранее выбранный объём продажи
(400 единиц) с реальным потолком рынка (давно выросшим), не перечитывая актуальную цифру каждый ход
— к моменту, когда спохватился (ход 14, продал в 10 раз больше), долг уже было не догнать.

**Потолок снижен до 0.1 (2026-08-19, тот же день, выбор пользователя, не угадано в одиночку):**
`MaxLoanInterestRate` 0.5 → 0.1 (10%/ход) во всех 4 файлах конфига — тот же метод клэмпа, число
пересмотрено. На долге, упёршемся в `MaxTotalDebt`, это ~1 000 000 в проценты за ход вместо 5 000 000
— всё ещё очень больно, но на порядок ближе к тому, что может пережить реальная команда, если поймает
проблему не слишком поздно. Живым прогоном новое число ещё не проверено.

**Сложность:** ~~Низкая-средняя~~ — сделано (число выбрано, механизм не менялся).
**Важность:** Средняя-высокая — два обвала за один день на разных моделях с разными триггерами
показали, что это не редкий сценарий при неразумной игре.

---

## Контент и обучение

### 15. Стартовая документация/подсказки для игроков

Раздел user guide с примерами — что вообще происходит, как принимать решения, на что смотреть в
первую очередь. Сейчас у новых участников нет ничего, кроме самого интерфейса.

**Сложность:** Низкая — контентная задача, не код; можно писать параллельно с чем угодно другим.
**Важность:** Высокая — на реальном мероприятии новые участники сейчас не получают вообще ничего,
кроме интерфейса; дёшево сделать, ощутимо снижает трение первого захода и нагрузку на ведущего,
которому иначе приходится объяснять всё голосом каждой команде отдельно.

**Решение по очерёдности (2026-08-16):** несмотря на «Низкая»/«Высокая», сознательно откладывается в
самый конец очереди, а не берётся в работу просто потому, что дёшево и важно — любая игрок-facing
документация должна писаться про уже устоявшийся интерфейс/механики, не раньше. Если написать её
сейчас, а потом (пункты 3, 5, 7, 9 и другие) поменять экраны/формулы, придётся переписывать документацию
заново — двойная работа вместо одной. Правило общее, не только для этого пункта (см. пункт 6 —
внутренний документ модели, отдельный случай: он не user-facing и нужен как предпосылка для пункта 3,
а не как финальная полировка, поэтому под это правило не подпадает).
